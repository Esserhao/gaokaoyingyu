# 方法规范 → 方案三（全协议移植）升级路线

> 状态：待办（未排期）。2026-09-07 M1 方案二落地后留档，作为后续升级的施工图。
> 参考原型：Stumax（shanghai-gaokao-english-os，浅克隆留档于 `.work/stumax-ref/`，临时目录可能被清）。

## 现状（方案二已交付）

- `data/methods/index.js` → `window.__METHODS__`：5 题型 × 24 步，每步 `id / name / action / forbid / fallback`。
- `#/methods` 门厅 + `#/methods/<key>` 单卡；主导航「学」域入口。
- 错题「错在哪一步」标签（`errStage` = step.id），列表编辑器 + 详情页两处可打点；错题本按阶段聚合（≥2 次才亮）。
- **铁律：step.id 就是错误标签词表，改 id = 断历史归因。**

## 升级到方案三要做的事

### 1. 规则引擎升级（数据层）
- 每步补齐 Stumax 的完整字段：`applies_when`（什么时候用）/ `not_when`（何时禁用，与 forbid 合并或分开）/ `failure_modes`（常见死法枚举，可与 forbid 合并）/ `version` / `status`。
- `status` 三态沿用 Stumax 口径：`PROVISIONAL`（可试用，默认）/ `VALIDATED`（须≥N 套真题验证步骤可复现）/ `PERSONALIZED`（个人复盘后固化）。UI 上如实标注，不冒充官方。
- 步骤 id 保持不变（向后兼容历史 errStage）。

### 2. 训练循环页（新路由）
- `#/methods/<key>/loop`：做题前过一遍流程卡 → 做题 → 对答案时逐步自评「这步做到了吗」→ 把没做到的步骤写回 errStage → 下次做题前只提示薄弱步骤。
- 与「分题型训练」联动：训练页卷头加「考前 2 分钟」方法卡折叠块（可复用每卷考点词折叠块的模式）。
- 周计划（Stumax 的 daily-weekly-cycle）：每周一个薄弱步骤做专项，来源直接取错题本阶段聚合的第一名。

### 3. 来源与验证登记
- `data/methods/SOURCES.md`：每条步骤登记依据类型（自著候选 / 真题复盘归纳 / 教辅参照），沿用全站「source 不得编造」铁律。
- counterexample 记录：某步被自己做题实战推翻时，在数据里记反例（Stumax 的 counterexample_search 思路），推动 status 从 PROVISIONAL → VALIDATED 或回炉改写。

### 4. 扩题型
- 主观题（proofreading / writing_app / writing_cont）目前无方法卡；补齐后 errStage 词表自然扩展，注意聚合页与 chips 的 sectionKey 联动已按「无卡不出」设计，无需改码。

## 明确不搬（Stumax 有但我们不要）

- Astro/PWA 技术栈与 CSV + Python 记录流（我们是纯前端 + localStorage，#15 导出导入已覆盖）。
- 多人协作的「第三方答案不得升级规则」等裁决流程——个人使用无此场景，但「主观题答案不进正确率」口径已在 G2 落实。

## 三句可进 UI 文案的金句（来自 Stumax，改写可用）

1. 「粗心不是诊断，拆成可观察动作。」
2. 「订正不等于迁移。」
3. 「高信心的错误优先处理。」
