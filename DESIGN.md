# 高考英语真题在线 · 设计锚（DESIGN.md）

> **本文件是全站视觉的唯一事实源。** 任何页面、任何会话轮次的样式改动都以它为准；
> 改风格 = 先改本文件，再改代码。它取代此前两份文档：
> `DESIGN_PLAN.md`、`DESIGN_OPTIMIZATION.md`（二者保留作历史决策记录，不再作为依据）。
>
> - 确立日期：2026-08-31
> - 选定方向：**C · 铅字印刷**（四方向预览 `design-previews/2026-08-31-fullsite-redesign/`）
> - 用户追加调整：**加上右侧悬浮的答题卡**（原为方向 D 的记忆点，现并入 C）
> - 三拨盘：视觉冒险度 **4/10** · 动效强度 **3/10** · 信息密度 **8/10**
> - 另两条已定前提：视觉气质 = 学术卷面·暖纸感；配色 = **统一中性 + 单一重音**，取消分区变色

---

## 1. 视觉主题与氛围

**一句话气质**：1980 年代油印试卷——米黄纸、油墨黑、双线报头、铅字方括号题号。
读者翻开它应当感到「这是一份正式考卷」，而不是「这是一个网站」。

三个不可妥协的感受：

1. **纸感**——底色必须是米黄纸 `#f0e9d8`，不是白，也不是冷灰。所有表面都是纸的深浅，
   没有玻璃、没有渐变、没有发光。
2. **油墨感**——文字是印上去的，不是发光的。标题与分隔用「双线」表达庄重，
   层级靠纸色差与线，不靠阴影。`box-shadow` 只允许出现在真正的浮层（抽屉/弹层/悬浮答题卡）。
3. **正式感**——题号一律等宽方括号 `[ 21 ]`，计时与统计一律等宽表格数字。
   数字不许抖（`tabular-nums`），这是考场的纪律感。

**暖纸感与 C 的关系**：C 是暖纸感家族内最硬的一支——同为暖中性，但用印刷规则线
（而非卡片与阴影）组织信息，密度最高。

---

## 2. 色板与角色

### 纸与墨（中性骨架）

| 令牌 | 值 | 角色 | 说明 |
|---|---|---|---|
| `--paper-0` | `#f0e9d8` | 页面底 | 米黄纸，品牌基底，**不用纯白** |
| `--paper-1` | `#f7f2e7` | 卡片 / 抬升面 | 比底色浅一档 |
| `--paper-2` | `#fbf8f1` | 最高面 | 输入框、弹层、悬浮答题卡 |
> 对比度为 **WCAG 2.1 实测值**，格式为 `纸底 #f0e9d8 / 卡面 #f7f2e7`。
> 全部数值由脚本计算得出，**不得凭观感估写**——本项目曾把 `--ink-3` 写成
> 「≈4.6:1 仍达 AA」，实测只有 3.32:1。改色值前重跑对比度计算。

| 令牌 | 值 | 角色 | 实测对比度 |
|---|---|---|---|
| `--ink` | `#1c1917` | 主文字 | 油墨黑，非纯黑　14.45:1 / 15.66:1　AAA |
| `--ink-2` | `#57534d` | 次要文字 | 6.31:1 / 6.84:1　AA |
| `--ink-3` | `#6e695f` | 三级 / 元信息 | 4.51:1 / 4.89:1　AA |
| `--line` | `#d6ccb4` | 常规分隔线 | 1.32:1　装饰性，不承载信息 |
| `--line-strong` | `#bdb096` | 强调分隔 | 1.77:1　装饰性，不承载信息 |

> 校准记录：`--ink-3` 原 `#857e73` 实测 **3.32:1 不达 AA**，已压暗至 `#6e695f`。

### 重音（全站唯一）

| 令牌 | 值 | 角色 | 实测对比度 |
|---|---|---|---|
| `--accent` | `#24503f` | **深墨绿**：题号、选中态、进度、链接、主按钮 | 7.56:1 / 8.20:1　AA |
| `--accent-soft` | `#e3ece4` | 重音底：选中项背景、高亮行 | 底，非文字 |
| `--accent-line` | `#637c6c` | 重音描边：选中边框、聚焦环辅助 | 3.75:1 / 4.06:1　非文本 ≥3:1 |

> 校准记录：`--accent-line` 原 `#9db8a8` 实测 **1.76:1**，选中态边框几乎看不见，
> 已压暗至 `#637c6c`。描边是选中反馈的唯一视觉载体，必须留足余量，不能贴 3:1 下线。

> 全站**只有一个重音色**。原分区配色（平台青 `#268b71` / 题库蓝 `#1a56db` /
> 训练锈红 `#b84c31` / 档案蓝 `#2d67b1` / 词缀紫 `#8a5b9b`）**全部退役**，
> 统一到墨绿。分区识别改由「报头文字 + 位置 + 图标」承担，不再靠变色。

### 功能色（语义固定，不参与装饰）

| 令牌 | 值 | 角色 | 实测对比度 / 使用约束 |
|---|---|---|---|
| `--red` | `#a3352b` | 错误 / 判错 | 5.59:1 / 6.06:1　**必配 ✗ 图标或「错误」文字** |
| `--red-soft` | `#f7e8e5` | 错误底 | 仅小面积 |
| `--green` | `#187746` | 正确 / 判对 | 4.61:1 / 4.99:1　**必配 ✓ 图标或「正确」文字** |
| `--green-soft` | `#e6efe4` | 正确底 | 仅小面积 |
| `--amber` | `#8a6212` | 警示 / 待订正 | 4.52:1 / 4.90:1　贴线达标，**勿再调浅** |

> 校准记录：`--green` 原 `#1a7f4b` 实测 **4.15:1 不达 AA**，已压暗至 `#187746`。

**重音墨绿 vs 判分绿的区别**（易混，必须守住）：
`--accent #24503f` 是**深沉低明度**的结构色（题号、进度条、链接），L = 0.065；
`--green #187746` 是**高明度**的语义色，只出现在判分标记上，L = 0.138。
二者相对亮度差 0.073，判分色永不单独表意（无图标/文字不得出现）。

**语义底上的文字**：`--ink` / `--ink-2` / `--ink-3` / `--accent` / `--green` / `--red` / `--amber`
在四种语义底（`accent-soft` / `green-soft` / `red-soft` / `amber-soft`）上**全部 ≥ 4.52:1**，
可放心叠字，无需为语义底另配文字色。

---

## 3. 排版规则

### 字体（全部系统栈，零 webfont）

```css
--font-serif: Georgia, "Times New Roman", "Songti SC", STSong, SimSun, serif;
--font-sans:  -apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB",
              "Microsoft YaHei", "Noto Sans SC", sans-serif;
--font-mono:  ui-monospace, SFMono-Regular, Menlo, Consolas, "Courier New", monospace;
```

| 字体 | 用在哪 |
|---|---|
| **衬线** | 英文题干、阅读正文、章节标题、卷名、分数大数字——一切「被阅读」的内容 |
| **无衬线** | 按钮、导航、标签、表格、元信息——一切「被操作」的 UI |
| **等宽** | 题号 `[ 21 ]`、计时器、统计数字、进度百分比——一切数字 |

> **禁止装饰性中文字体**（思源/ZCOOL 全量引入均违规）。中文个性靠字重、字号、
> 排版结构与色彩实现，不靠字体文件。中文没有等宽字形，等宽只用于数字与 ASCII。

### 字阶（16px 基准）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--fs-3xs` | 11px | 角标、页脚注 |
| `--fs-2xs` | 12px | 标签、辅助说明 |
| `--fs-xs` | 13px | 次要正文、表格 |
| `--fs-sm` | 14px | UI 正文（中文下限） |
| `--fs-md` | 15px | 正文基准 |
| `--fs-lg` | 17px | 英文题干 |
| `--fs-xl` | 20px | 小标题 |
| `--fs-2xl` | 24px | 区块标题 |
| `--fs-3xl` | 30px | 页标题 |
| `--fs-4xl` | 38px | 首页大标题 |

### 行高与字重

| 场景 | 行高 | 字重 |
|---|---|---|
| 中文正文 / 题干 | **1.7** | 400 |
| UI 文本 | 1.45 | 400 / 500 |
| 标题 | 1.25–1.35 | **500 或 600**（不写 `bold`） |
| 等宽数字 | 1.2 | 500 |

- 中文字重**只用 400 / 500 / 600**（多数中文字体无中间字重，伪合成发虚）
- **禁用斜体**（含 `font-style: italic`；中文无斜体字形，强制倾斜即笔画变形）
- **禁用负字距**；标题可 +0.02em，大标题最多 +0.05em
- 中文与英文/数字之间加空格（盘古之白）：`共 40 题`、`第 21 题`
- 数字与单位间加空格（`20 MB`），例外：`90°`、`15%` 不加

---

## 4. 组件样式

### 按钮

| 变体 | 底 | 字 | 边框 | 圆角 |
|---|---|---|---|---|
| 主按钮 | `--accent` | `--paper-2` | 无 | `--r-sm` (3px) |
| 次按钮 | `--paper-2` | `--ink` | 1px `--line-strong` | `--r-sm` |
| 文字按钮 | 透明 | `--accent` | 无 | `--r-sm` |

- 高度 ≥ 44px（触控目标），内边距 `10px 16px`
- 按下反馈 `transform: scale(.99)`，120ms，**不用 `transition: all`**
- 禁用态：`--ink-3` 文字 + `--line` 底，不加透明度 hack

### 选项（答题核心控件）

- 默认：`--paper-1` 底 + 顶部 1px `--line` 分隔（不是卡片，是列表行）
- 悬停：底变 `--paper-2`
- 选中：**整块 `--accent-soft` 底 + 1px `--accent-line` 全边框 + 左侧实心方块 `■`**
- **禁止**：左侧竖线装饰（见 §7 Don't）

### 卡片与容器

- 底 `--paper-1`，1px `--line` 边框，**圆角 `--r-md` (4px)**，**无阴影**
- 只有抽屉、弹层、悬浮答题卡可用 `--shadow-3`
- 卡片内边距 16–20px（密度 8/10，不用 24–32px 的宽松值）

### 报头与章节

- 卷名报头：`border-bottom: 3px double var(--ink)`（双线，C 的签名）
- 章节头：1px `--line-strong` + 全角间隔的标题字（`英 语 · 新 课 标 I 卷`）

### 全站主导航（2026-09-02 新增）

- 五域常驻条：**学 / 练 / 错 / 词 / 工具**，`details/summary` 原生下拉（零 JS、键盘可用），随 `header()` / `referenceHeader()` 渲染于每页报头之下
- 域条：`--paper-1` 底 + 上下 1px `--line`；域名字重 700，`--ink`，其余 `--ink-2`
- 当前域高亮：整块 `--accent-soft` 底 + inset 底部 2px `--accent`（遵守 P-38：不用左竖线）
- 下拉菜单：`--paper` 底 + 1px `--line` 线框，**无投影**（铅字印刷，分层靠线框）；悬停项 `--accent-soft`
- 移动端 ≤720px：五域平铺一行、菜单就地展开（不悬浮），触控目标 44px
- 豁免页：答题页（examBar）、同义词星空画布——保持作答/探索沉浸

### 输入框

- 底 `--paper-2`，1px `--line-strong`，圆角 `--r-sm`，内边距 `8px 12px`
- 聚焦：2px `--accent` 边框（不用发光）
- 数字输入一律 `font-variant-numeric: tabular-nums`

### 答题卡（用户指定：右侧悬浮）

- 桌面 ≥ 960px：**右侧悬浮栏**，宽 132px，`--paper-2` 底 + 左侧 1px `--line`
- < 960px：转为**底部抽屉**（沿用现有 `.answer-sheet` + `ui-sheet.js` 机制）
- 题格：21px 方，未答 `--paper-2` + `--line`，已答 `--accent`，当前 `--accent` 描边空心
- 已答题格的完成态用 `--accent` 填充，**不引入金色**（成就金退役）

---

## 5. 布局原则

- 容器：内容页 `max-width: 1180px`；试卷页 `1240px`；长文阅读 `max-width: 68ch`
- **分栏只给短内容**：选项区、说明区、对照区可用 2 栏（含栏间 1px 竖线）
- **阅读正文一律单栏 + 左对齐**（见 §7 Don't——这是可读性硬约束）
- 栅格：4px 基准，组内间距 < 组间间距；章节间距 96px
- 信息密度 **8/10**：列表行紧凑（padding 10–12px），元信息与主体同屏可见

### 悬浮答题卡的布局代价（已评估）

右侧 132px 悬浮栏会从内容区拿走宽度。因此：
- 试卷页内容区在 ≥960px 时仍保持 ≥ 620px（阅读正文 68ch ≈ 620px，够用）
- < 960px 立即转抽屉，不挤压正文

---

## 6. 深度层级

不用阴影堆叠，用**纸色 + 线**表达层级（印刷逻辑）：

| 层 | 表现 | 用途 |
|---|---|---|
| L0 | `--paper-0` 无边框 | 页面底 |
| L1 | `--paper-1` + 1px `--line` | 卡片、列表容器 |
| L2 | `--paper-2` + 1px `--line-strong` | 输入框、选中项、悬浮答题卡 |
| L3 | `--paper-2` + `--shadow-3` | 抽屉、弹层（唯一允许阴影的层） |
| 强调 | `3px double var(--ink)` | 卷名报头、章节分隔（签名动作） |

---

## 7. Do's 与 Don'ts

### Do

- 纸底 `#f0e9d8` 是品牌基底，永远不用纯白作页面底
- 题号用等宽方括号 `[ 21 ]`，计时/统计用等宽表格数字
- 章节分隔用 3px 双线，这是全站最重的一笔
- 中文正文行高 ≥ 1.7，字号 ≥ 14px
- 判分色必配图标或文字，不以色为唯一信息载体
- 圆角保持 2–6px，方形感是印刷气质的来源

### Don't（违反即返工）

- ❌ **禁止任何「左侧竖线」装饰选中态或卡片强调**——包括 `border-left` 色条、
  `box-shadow: inset Npx 0 0`、伪元素左侧竖线。选中与强调用「整块底色 + 全边框 / 字重」表达。
  （用户长期偏好 P-38；现有 `platform.css` 的 `.dashboard-hero{border-left:4px}` 已在清理名单）
- ❌ 禁止阅读正文使用 `text-align: justify` + 分栏——中英混排会产生词间河流，
  且高考阅读正文 300+ 词，可读性优先于气质（P-5）
- ❌ 禁止斜体、禁止装饰性中文 webfont、禁止负字距
- ❌ 禁止圆角 > 8px（胶囊标签 `--r-pill` 除外）
- ❌ 禁止重音色作大面积背景；禁止引入第二个重音色
- ❌ 禁止 `transition: all`；禁止 `ease-in` 曲线；禁止入场动画从 `scale(0)` 起
- ❌ 禁止暗色孤岛：星座页（`#/synonyms`）的夜空是「有意的夜间章节」，
  但必须复用本文件的字阶、间距、圆角与墨绿重音令牌，不得自成一套

---

## 8. 响应式行为

| 断点 | 变化 |
|---|---|
| ≥ 1200px | 分栏全开 + 右侧悬浮答题卡（132px） |
| 960–1199px | 分栏保留 + 悬浮答题卡保留 |
| 768–959px | **分栏塌缩为单栏**，答题卡转底部抽屉 |
| < 768px | 全单栏；答题卡抽屉（沿用 `.answer-sheet.open` 机制）；正文 16–18px |

- 触控目标 ≥ 44px（现有 `design-system.css` 已达标，保留）
- 非对称布局在 < 768px 一律塌缩为单列
- 不用 `h-screen`，用 `min-height: 100dvh`

---

## 9. Motion 哲学（强度 3/10）

**原则：做题是每天上百次的高频操作，动效只长在「有叙事」的地方。**

允许动效的三处（且仅此三处）：
1. **交卷判分**：结果卡 280ms 淡入上移（`--dur-slow`）
2. **抽屉 / 悬浮答题卡展开**：280ms transform
3. **页签切换**：180ms（`--dur-base`）

**禁止动效的**：选选项、跳题、翻页、输入、勾选——这些每天触发上百次，一动就是负担。

```css
--dur-fast: 120ms;   /* 按钮按压 */
--dur-base: 180ms;   /* 展开收起、页签 */
--dur-slow: 280ms;   /* 抽屉、转场 */
--ease-out: cubic-bezier(0.23, 1, 0.32, 1);
--ease-inout: cubic-bezier(0.77, 0, 0.175, 1);
```

- 只动 `transform` 与 `opacity`
- 全站 `prefers-reduced-motion: reduce` 兜底（现有 `design-system.css` 已有，保留）
- 任何 UI 动效 ≤ 300ms

---

## 附录 A · DNA 注入来源

| 来源 | 偷到的具体值 |
|---|---|
| `claude/DESIGN.md` | 暖纸底 `#f5f4ed`→本地化为 `#f0e9d8`；Near Black `#141413`→`#1c1917`（油墨黑）；**全暖中性、零冷灰**；正文行高 1.60→中文上调为 1.70；衬线标题单一字重 500 |
| `chinese-typography.md` | 中文行高 1.5–1.75；字重只用 400/500/600；盘古之白；数字 `tabular-nums`；暗底正文不纯白 |
| `user-preferences.md` P-38 | 禁左侧竖线装饰——直接改写选中态方案（改为整块底色 + 全边框 + `■`） |
| `user-preferences.md` P-5 | 可读性优先——直接改写 C 的报纸分栏（阅读正文退回单栏左对齐） |
| 用户追加调整 | 右侧悬浮答题卡（原属方向 D）并入 C，< 960px 转底部抽屉 |

## 附录 B · 落地顺序（每段跑一次 45 路由快照护栏）

1. **令牌层**：`css/style.css` 的 `:root` 按本文件重写，全站色值收敛到令牌
2. **组件层**：按钮 / 选项 / 卡片 / 输入框 / 报头 / 答题卡统一
3. **收敛旧模块**：`platform.css`（301 处 hex）、`training.css`（81 处）、`reference.css`（86 处）
4. **布局层**：试卷页加悬浮答题卡；首页与仪表盘按 8/10 密度重排；清理左侧竖线装饰
5. **星座页对齐**：复用字阶/间距/圆角/重音令牌，夜空作为「夜间章节」保留

---

## §10 签名动作（本次重构新增，2026-08-31）

全站「被记住的东西」必须落在以下签名上，禁止再加装饰性色块/发光/渐变：

1. **纸纤维氛围层**（`body::before`，`css/style.css`）：固定 `feTurbulence` 噪点，
   `opacity:.04` + `mix-blend-mode:multiply`，不滚动、不挡交互、不承载信息。
   仅给「这是一份油印卷面」的触觉，绝不影响正文对比度（已实测）。
2. **选区高亮**（`::selection`）：油墨黑底 + 纸面反白字，全站一致。
3. **双线报头**（`3px double var(--ink)`）：C 方向最重的一笔。`.section-heading`
   与卷名报头共用；新增分隔用 `.rule-double` 工具类。
4. **成绩印章**（`#/result` 的 `.score-ring`）：由 spinner 式单色环改为
   **闭合双环绿印章**（墨绿底 + 3px 墨绿线 + 外圈 1px 细环 via `::after`），
   像一枚盖在卷面上的骑缝章；大数字仍用衬线 + `tabular-nums`。
5. **共享状态**：空态 `.state-empty` / 错误态 `.state-error` / 加载态 `.is-skeleton`
   （微光只动 `opacity`，遵守降级）。页面取数、无结果、出错时挂对应类，反馈全站一致。

### 新增令牌（同次重构）
- `--on-accent: #fbf8f1` / `--on-ink: #fbf8f1`：文字压在重音/油墨实色上时用，
  取代裸 `#fff`，更「印上去」而非「发光」；旧 `--blue` 别名在 CSS 设计层已全部
  改回 `--accent*`（别名仅保留给 6000 行 JS 兼容，不再出现在样式文件）。
- 退役色清理：`training.css` 旧「训练锈红 `#b84c31`」仅存于注释；实际规则统一到
  `--red` / `--accent`。`design-system.css` 旧「成就金 `--gold`」引用全部改回 `--accent*`。

### 认证状态
- 设计层（app/design-system/learn/training/word/style/responsive）非令牌 hex = 0；
  残留 hex 均为夜空调色板（synonyms.css）、校准史注释、SVG 白字标签与 `--lv-3:#fff`，
  均属有意，不改。
