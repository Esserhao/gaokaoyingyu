# 高考英语真题录入工作流

面向后续接手此项目的 AI/人工。目标：把一套高考英语真题从原始文档（.doc/.docx/.pdf）
手工录成 `data/exams/<id>.json`，并保证它在网站上能正确渲染、正确判分、不泄漏答案。

**前提约定：逐套手工录入，不写批量生成器。** 项目早期的自动解析脚本
（`tools/pipeline/*.py`）产出了 16 套结构损坏的数据（题号重复、答案丢失、
答案泄漏进正文），这是本文档存在的原因。不要重走这条路。

---

## 0. 快速开始

```powershell
# 0. 盘点源文档，挑 ready 的先做
python tools/survey_sources.py
# 1. 抽取源文档文本（自动处理 docx/doc/pdf，PDF 自动判定是否需 OCR）
python tools/extract_source.py "E:\Desk\题目\<源文件>" --out 2023/new1-jiexi.txt
# 2. 手工编写 data/exams/<id>.json（见第 2 节格式规范）
# 3. 校验 + 自动修复
python tools/exam_check.py <id> --fix
# 4. 同步 data/index.json，再跑一次确认全绿
python tools/exam_check.py <id>
```

三个脚本各司其职：`survey_sources.py` 决定**先做哪套**，
`extract_source.py` 负责**拿到干净文本**，`exam_check.py` 负责**验收**。

---

## 0.5 录入优先级（由 survey_sources.py 得出）

源目录 `E:\Desk\题目` 共 31 个文档，按可用性分四档：

| 档位 | 数量 | 含义 | 策略 |
|---|---|---|---|
| `ready` | 7 | .docx 解析版，含【答案】【解析】【原文】 | **优先全部录完** |
| `no-ans` | 9 | .docx 原卷版，无逐题答案 | 需另找答案源，暂缓 |
| `legacy` | 11 | .doc 老二进制格式 | 需先转 docx |
| `ocr` | 4 | PDF | 成本最高，最后做 |

**ready 档的 7 套**（2022 甲/乙/新高考Ⅰ，2023 甲/乙/新课标Ⅰ/新课标Ⅱ）是当前唯一
能一次录完、无需外部补答案的批次。**先把这 7 套做完**再碰其他档位。

值得注意的坑：`2024`、`2025` 的 5 个 .docx 全是**原卷版**——survey 里显示
"仅含听力例题的答案是C，无逐题答案"。它们看起来完整（有全部题目和文章），
但**没有任何一道题的答案**，硬录只能得到一份无法判分的残卷。
录之前必须先解决答案来源问题。

PDF 档（2021 新高考Ⅱ卷、2026 全国Ⅰ卷）已确认文本层损坏，
`extract_source.py` 会自动转 OCR（rapidocr，300dpi，约 8–13 秒/页），
识别质量可用但仍需逐处校对英镑符号、`l/1`、`O/0`。**成本明显高于 docx，排在最后。**

---


## 1. 权威格式来源

**schema 的唯一权威是前端代码，不是任何已有的 JSON 文件。**

| 关注点 | 权威文件 | 关键位置 |
|---|---|---|
| 题目渲染、选项、解析展示 | `js/render.js` | `question()`、`proofreadingQuestion()`、`explanationHtml()` |
| 判分逻辑、错题本入库 | `js/exam.js` | `score()`、`submit()` |
| 专题训练渲染 | `js/topic.js` | `renderSeven()` 等 |
| 索引字段 | `js/render.js` | `paperCard()`、`paperRow()`、`reviewBadge()` |

**不要**参考 `data/exams/` 里 2022–2026 的旧文件当模板——它们是坏数据。
干净样板：`gk2021-jia.json`（旧课标结构）、`gk2021-xgk1.json`（新课标结构）。

---

## 2. JSON 格式规范

### 2.1 顶层

```json
{
  "id": "gk2021-xgk1",
  "year": 2021,
  "paper": "新高考Ⅰ卷",
  "region": "山东、河北、湖北、湖南、江苏、广东、福建",
  "title": "2021年新高考Ⅰ卷英语",
  "format": "new",
  "duration": 120,
  "totalScore": 150,
  "hasAudio": true,
  "audio": "audio/listening-2021-xgk1.mp3",
  "quality": "verified",
  "proofreadingConfirmed": true,
  "sections": [ ... ]
}
```

- `format`：`legacy`（旧课标，有短文改错）/ `new`（新课标，有读后续写）。
- `audio` 必须是**字符串**路径且文件真实存在（`exam_check` 会核实）。
  甲、乙卷共用 `audio/listening-2021-jiayi.mp3`。
- `proofreadingConfirmed: true` 只在短文改错已逐处人工核对后才写。

### 2.2 section

```json
{
  "key": "reading",
  "partTitle": "第二部分 阅读（共两节，满分50分）",
  "secTitle": "第一节（共15小题；每小题2.5分，满分37.5分）阅读下列短文…",
  "passages": [ { "label": "A", "text": "…" } ],
  "pool": [],
  "questions": [ ... ]
}
```

`key` 只能取这 7 个值，且**按此顺序**排列：

`listening` → `reading` → `seven` → `cloze` → `grammar` → `proofreading` → `writing_app` / `writing_cont`

- `passages[].label` 用 A/B/C/D…，题目通过 `passageLabel` 关联。
  七选五、完形、语法填空各自的文章也要放进 `passages` 并给 label（延用 E/F/G…）。
- `pool` 只有 `seven` 用（7 个选项），其余留空数组。

### 2.3 question

```json
{
  "id": 21,
  "passageLabel": "A",
  "stem": "What is probably the major concern…?",
  "options": [ { "letter": "A", "text": "Comfort." } ],
  "answer": "C",
  "type": "reading",
  "score": 2.5,
  "explanation": {
    "summary": "细节理解题｜本文是应用文，介绍…",
    "question": "问选择住旅社的旅行者最可能关心什么。",
    "evidence": "Rome can be pricey for travelers…",
    "answer": "C。罗马物价昂贵正是许多人选择住旅社的原因。"
  }
}
```

硬性规则：

1. **`id` 用高考官方连续编号，全卷 1…N 不重复、不分段重排。**
   旧课标卷 1–72，新课标卷 1–67。填空型题目的 `stem` 写 `"第41空"`，
   正文里的空位标记写 `____41____`，两者数字必须与 `id` 一致。
2. **客观题必须有 `answer`；主观题必须有 `modelAnswer` 且不能有 `answer`。**
   `exam.js` 用 `q.answer == null` 判定"人工判分"，写了 `answer` 的题会被计入机器判分。
3. **`options` 必须逐题写全**，即使七选五的选项与 `section.pool` 重复（见 4.2）。
4. `explanation` 用结构化对象（`summary`/`question`/`evidence`/`answer`），
   短文改错用 `points[]`（见 2.5）。不要写成一整段字符串。
5. **答案与解析绝不能出现在 `passages[].text`、`stem`、`material` 里。**

### 2.4 分值表（照抄，两种卷型完全不同）

| section | 旧课标（legacy） | 新课标（new） |
|---|---|---|
| listening | 20 题 × 1.5 = 30 | 20 题 × 1.5 = 30 |
| reading | 15 题 × 2 = 30 | 15 题 × 2.5 = 37.5 |
| seven | 5 题 × 2 = 10 | 5 题 × 2.5 = 12.5 |
| cloze | 20 题 × 1.5 = 30 | 15 题 × 1 = 15 |
| grammar | 10 题 × 1.5 = 15 | 10 题 × 1.5 = 15 |
| proofreading | 1 题 × 10 | — |
| writing_app | 1 题 × 25 | 1 题 × 15 |
| writing_cont | — | 1 题 × 25 |
| **合计** | **72 题 / 150 分** | **67 题 / 150 分** |

### 2.5 短文改错的 explanation.points

```json
"explanation": {
  "summary": "短文改错 · 十处批改",
  "points": [
    {
      "number": 1,
      "errorType": "时态",
      "knowledgeNode": "动词时态",
      "operation": "改词",
      "wrong": "is",
      "correct": "was",
      "note": "全文叙述过去的事，用一般过去时。"
    }
  ]
}
```

`knowledgeNode` 会进错题本的知识节点统计（`render.js` 的 `knowledgeDetail`），
用统一词表：动词时态、主谓一致、词形变化、固定搭配、介词词组、冠词、连词、代词。

---

## 3. 录入步骤

### 3.1 抽取源文本

用 `tools/extract_source.py`，不要手写抽取脚本：

```powershell
python tools/extract_source.py "E:\Desk\题目\2023年新课标全国Ⅰ卷英语真题（含听力）（解析版）.docx" --out 2023/new1-jiexi.txt
```

输出到 `.work/extract/<out>`，并打印字符数与**可读率**。脚本已处理的事：

- `.docx` 走 python-docx，**同时抽正文段落和表格**（试卷的选项/活动时间表常在表格里，
  漏掉会丢题）。
- `.doc` 若实为 zip 容器则按 docx 解析；真老二进制格式会明确报错并列出可行替代
  （pywin32 / LibreOffice / 手工另存），**不静默产出空文件**。
- `.pdf` 先试文本层，用可读率自动判定；低于 55% 自动转 OCR。`--force-ocr` 可强制。
- 输出前统一做噪声归一：全角冒号 `7：40`→`7:40`、控制字符、`l0`→`10`、`5O`→`50`、
  `f12`→`£12`、`新高考T卷`→`新高考Ⅱ卷`。

**优先用解析版**：它同时含题目、答案、逐题解析和听力原文。原卷版只在核对选项排版时用。

解析版的答案块格式（2022/2023 的 7 套 ready 文档都是这个格式）：

```
3. 【此处可播放相关音频，请去附件查看】
When will the man see Bob?
A. This Friday.	B. This Saturday.	C. Next Monday.
【答案】B
【解析】
【原文】W: Mark came out of hospital last Friday. …
```

多题共用一个答案块时写作 `【答案】6. C    7. B`。听力 transcript 就在 `【原文】` 后。

抽出的文本仍有残余噪声，录入时逐处修正：

| 噪声 | 例子 | 处理 |
|---|---|---|
| OCR 错字（仅 PDF） | `terible`、`Bemardsville`、`kithen` | 还原正确拼写 |
| 智能引号 | `we’11 go`（实为 `we'll`） | 还原为 `'ll` |
| 选项丢字母 | `To teach Spanish.` 缺开头 `A.` | 补回 letter |
| 音频占位符 | `【此处可播放相关音频，请去附件查看】` | 删除，不进 JSON |
| 答题横线 | 长串 `_____` | 删除 |


### 3.2 手工编写 JSON

**分批写，不要一次写完整个文件。** 单次 write_file 内容过大会被截断。
稳定做法：

1. `write_file` 先写顶层 + `listening` section，结尾闭合 `] }`。
2. 之后每个 section 用 `patch` 追加：`old_string` 取上一 section 的末尾
   （最后一题的 `"answer"` 行 + `] } ] }`），`new_string` 换成
   `…] }, { 新 section } ] }`。
3. 每追加 1–2 个 section 就跑一次 `python tools/exam_check.py <id>`，早发现早改。

听力 transcript 放在每个材料组的**首题**上，同组后续题只写 `group` 字段
（如 `"group": "听下面一段较长对话，回答第6至7小题。"`）。

### 3.3 校验与修复

```powershell
python tools/exam_check.py <id> --fix
```

`--fix` 目前只自动修一件事：把 `section.pool` 同步进七选五每题的 `options`。
其余 FAIL 都需要手工改。

### 3.4 同步 index.json

`data/index.json` 里对应条目必须与试卷本体一致：

```json
{
  "id": "gk2021-xgk1",
  "questionCount": 67,
  "totalScore": 150,
  "file": "exams/gk2021-xgk1.json",
  "hasAudio": true,
  "quality": "verified"
}
```

- `questionCount` 是**全卷题数**（不是听力题数）。旧数据里普遍错写成 20。
- 录完把 `quality` 从 `review_required` 改为 `verified`。
- `region` 写实际适用省份，不要留"新高考地区"这种占位。

---

## 4. 已知陷阱

这些都是实跑校验暴露出来的真实缺陷，不是理论风险。

### 4.1 题号分段重排会撞号

按源文档"阅读第1题、完形第21题"分段编号，会让 `listening` 的 1–20 与
`reading` 的 1–15 撞车 20 个。`exam.js` 的答案 key 是 `${section.key}-${q.id}`，
section 不同不串答案，但答题卡侧栏和错题本按题号显示，会出现两组"1…20"。
**必须用全卷连续编号。**

### 4.2 七选五只写 section.pool 会退化成填空框

`render.js` 的 `question()` 和 `topic.js` 的 `renderSeven()` **只读 `q.options`，
完全不读 `section.pool`**。只在 section 级放 pool，题目会被渲染成
"请输入答案"文本框。每套新卷都会踩这个坑，`--fix` 可自动补。

### 4.3 render.js 曾用 `q.id>=50` 硬判主观题

旧代码 `const free = q.type==='writing' || (!q.options?.length && q.id>=50)`
在改用连续编号后会把第 50 题（语法填空）误判成作文而渲染文本域。
已改为只按 `q.type==='writing'` 判断。**若再看到基于 id 数值的类型判断，是 bug。**

### 4.4 年份目录旧数据把答案塞进 material

`tools/pipeline` 的自动解析把【答案】【解析】整段写进了 `material`/`passage`，
练习模式下学生直接看到答案。`exam_check.py` 的 `check_leaks` 专门拦这个。
**不要从这些文件复制正文。**

### 4.5 源文件名可能与实际卷别不符

2021 全国乙卷的源文件名是「新课标Ⅰ解析版.doc」，但内容是乙卷
（有短文改错 → 旧课标结构；新高考卷没有短文改错）。
**按内容结构判断卷别，不信文件名。**

### 4.7 源文档可能根本没有答案

2024/2025 的 5 个 .docx 是原卷版，全卷零答案，但表面看很完整（题目、文章、
选项都在）。录到一半才发现无法判分，等于白做。
**动手前先跑 `survey_sources.py`，只碰 `ready` 档。**

### 4.8 PDF 文本层可能是垃圾

2021 新高考Ⅱ卷的两个 PDF 字体子集化 + ToUnicode 表错位，`page.get_text()`
抽出的是 `!"#$%&'()` 符号流（可读率约 10%）。`extract_source.py` 用可读率
自动判定并转 OCR。**不要拿低可读率的文本当有效内容录入。**

### 4.9 PowerShell / 环境相关

- 本机 `terminal` 走 PowerShell 5.1。行内 `python -c "..."` 会被解析器
  搞坏（f-string 里的花括号方括号），一律写成 `.work/*.py` 再执行。
- `Select-String` / `Format-Table` 管道常报 exit_code 1 但 stdout 完整，看输出别看退出码。
- `[IO.File]::ReadAllText` 用相对路径会相对进程 cwd 解析，传绝对路径。

---

## 5. 校验脚本分工

| 脚本 | 用途 | 何时跑 |
|---|---|---|
| `tools/survey_sources.py` | 盘点源目录，按 ready/no-ans/legacy/ocr 分档定优先级 | 开始新批次前 |
| `tools/gap_report.py` | 全库逐套校验 + 按源可用性分组重写任务 + 音频盘点 | 想知道"还缺什么" |
| `tools/extract_source.py <src> --out <rel>` | docx/doc/pdf → 纯文本，自动 OCR 兜底 + 噪声归一 | 每套录入前 |
| `tools/exam_check.py <id> --fix` | 单套：结构+判分+泄漏+渲染+index，可自动修 | 每录完一个 section / 一套 |
| `tools/exam_check.py --all` | 全库回归，确认没改坏别的 | 一套录完后 |
| `tools/quality_audit.py` | 全库静态体检，输出逐条 issue JSON | 想看残缺卷的详细缺陷时 |

`exam_check.py` 取代了早期分散的三个脚本（`.work/verify_exam.py`、
`score_check.js`、`.work/fix_seven_options.py`），且不再依赖 Node。
`extract_source.py` 取代了一次性的 `.work/pdf_extract.py` / `.work/pdf_ocr.py`。

判分校验的原理：用标准答案模拟一次满分作答，复刻 `exam.js` 的 `score()`，
确认「客观题满分 + 主观题分值 = totalScore」。这能一次性抓出分值写错、
answer 缺失、answer 与 options 不匹配、主观题误标 answer 等问题。

### 依赖

```powershell
python -m pip install python-docx pymupdf==1.24.14 rapidocr-onnxruntime
```

`rapidocr-onnxruntime` 只在处理损坏 PDF 时需要；纯 docx 流程不需要它。
本机无 LibreOffice、无 pywin32，所以 `.doc` 档位目前走不通。


---

## 6. 进度

已按本规范录完并全绿：

| id | 卷别 | 题数 | 结构 |
|---|---|---|---|
| `gk2021-jia` | 2021 全国甲卷 | 72 | legacy |
| `gk2021-yi` | 2021 全国乙卷 | 72 | legacy |
| `gk2021-xgk1` | 2021 新高考Ⅰ卷 | 67 | new |

待录入（`data/exams/` 下现有文件均为坏数据，需整套重写）：
2021 新高考Ⅱ卷、2022 四套、2023 四套、2024 三套、2025 两套、2026 全国Ⅰ卷。

`python tools/exam_check.py --all` 当前：19 套中通过 3、失败 16。
这个数字随录入推进下降，是进度的客观指标。
