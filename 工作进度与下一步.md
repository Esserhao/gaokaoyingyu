# 工作进度与下一步

> 生成时间：2026-08-22
> 上一轮工作截止于 #13 间隔复习完成并全护栏通过。
> 本文件是「断点续接」用的：过几天回来先看这里，不必重读 overview.md 全文。
> 逐轮的详细技术记录在 `overview.md`（追加式日志），产品路线图在 `task.md`。

---

## 一、本轮（2026-08-21 夜 — 08-22）已完成

三件事，按完成顺序。

### 1. 可读性重构收尾

`js/render.js` 拆成 `js/ui/` 五个文件（base / library / exam-page / dashboard /
mistakes），`topic.js`、`app.js`、`exam.js`、`training*.js` 重排。

护栏是**逐字节 DOM 快照**：37 路由 + 31 屏专题 + 2 屏草稿横幅。
只要输出 HTML 完全一致，就证明这次改动纯属可读性重排、没有行为变化。
比外观回归强得多 —— 外观只抽查若干属性，快照覆盖全部标记。

全站最宽行 109 列（`js/topic.js`），其余都在 108 以内（目标 ≤110，
按显示宽度算，中文计两列）。

### 2. #12 错因自动分类

新增 `js/diagnose.js`。交卷收错题时（`Exam.collectMistakes`）按题库自带的
`explanation.summary` / `explanation.question` 预填「错因 → 细分 → 知识节点」
三段口径，全库 **955 道客观题覆盖率 100%**。

为什么这件事重要：这三格此前是错因数据的**唯一**来源。学生不手填，
知识系谱就是空的、知识点详情页是空的、跨试卷重练分不出组 ——
整条「错题 → 错因 → 知识节点 → 回炉」的链路吊在一个没人执行的手动步骤上。

顺手修掉一个潜伏 bug：`js/app.js` 的 change 处理器没有监听 `[data-node]`，
知识节点输入框改了从来没保存过。

### 3. #13 间隔复习

新增 `js/review.js`（纯计算，不碰 DOM 也不读写 localStorage）。
**1 / 3 / 7 / 15 / 30 天**固定阶梯，新路由 `#/review-queue` 只回答一个问题：
今天该看哪几道。一条卡片只有题干、轮次和三个按钮，答案默认折叠 ——
展开着复习就退化成读解析。

没做 SM-2 是有意的：它要求每次复习给 0–5 的质量分，而本站的复习动作
只有三个按钮。用三档去驱动六级质量分，算出来的间隔精度是假的。

### 4. #15 跨设备进度导出/导入

把 `gkyy_records_v1` 整包导出成 JSON，导入时**合并**而非覆盖。

新增 `#/data` 路由页（「数据备份与迁移」），首页顶部 `home-tools` 也加了入口。
导出按钮把当前设备全部记录下载成一个带日期的 JSON；导入按钮选文件后与当前数据按规则合并：

- 整卷成绩 / 整卷草稿 / 专题草稿：按更新时间较新的一方保留；
- 错题：按 `(examId|qid)` 身份去重，间隔复习排期（`reviewStage` / `nextReviewAt` /
  `reviewStatus`）取**进展更靠前**的一方（毕业 > 任一阶段；同阶段取排期更远）；
- 写作草稿走独立 localStorage 键，**不**纳入这套机制，页面已明确告知。

关键风险点：导入绝不能把别台设备已排好的复习计划整批丢掉。护栏 `.work/sync.html`
（`SYNC-DONE total=11 fail=0`）专门验证：跨设备合并后第 4 轮 / 30 天后的排期仍在、
同题去重到 2 条不重复、解析坏文件不给全局报错。

改动文件：`js/store.js`（exportData / mergeData + 合并辅助）、`js/app.js`
（`#/data` 路由、export-data / import-data 动作、文件 change 处理、handleImport）、
`js/ui/dashboard.js`（UI.dataManager）、`js/ui/library.js`（home-tools 入口）、
`css/platform.css`（.sync-* 样式）、`.work/domdump.html`（路由表加 #/data）、
`.work/sync.html`（新增护栏）。

### 5. 九个护栏的最新状态（全绿）

```
.work/reviewq.html    REVIEW-DONE  total=26   fail=0
.work/diagnose.html   DIAG-DONE    total=955  auto=955  fail=0
.work/sync.html       SYNC-DONE    total=11   fail=0     （#15 导出/导入）
.work/sectioncheck.html  SECTIONCHECK-DONE  total=11  fail=0  （本轮新增，分题型得分）
smoke-test.html       DONE         total=66   fail=0
.work/a11y.html       A11Y-DONE    fails=0
.work/visual.html     VISUAL-DONE  total=25   fail=0
contrast-audit.html   AUDIT-DONE   routes=23  violations=0
37 路由 DOM 快照       仅 #/dashboard 一处 diff（能力概览 64%→待开始，种子无 sectionHistory），
                     其余 36 路由 IDENTICAL（已重取基线 snap-section-after.json）
31 屏专题快照          IDENTICAL
2 屏草稿横幅快照       IDENTICAL（已重取基线）
```

跑法（本地服务器 `python -m http.server 8731` 起在项目根目录）：

```bash
node --check js/<file>.js

rm -rf .work/cdpX && "/c/Program Files/Google/Chrome/Application/chrome.exe" \
  --headless=new --disable-gpu --no-sandbox \
  --user-data-dir="E:/Desk/高考英语网站/.work/cdpX" --virtual-time-budget=150000 \
  --dump-dom "http://127.0.0.1:8731/.work/domdump.html" 2>/dev/null \
  | PYTHONIOENCODING=utf-8 python .work/savesnap.py .work/snap-after.json
PYTHONIOENCODING=utf-8 python .work/cmpsnap.py
```

**读结果的规则**：只认 `<div id="out">` 里的头一行（`DONE` / `DUMP-DONE` /
`REVIEW-DONE` / `AUDIT-DONE` …）。拿 `grep FAIL` 扫整个 `--dump-dom` 输出会
命中护栏自己的源码，得到假失败。

---

## 本轮续做（2026-08-23）：分题型得分数据

仪表盘「能力概览」六行从共用单一粗略掌握度，改为按题型读历史得分聚合。

### 1. 改动

- `js/store.js`：新增 `addSectionHistory(exam, result)`（遍历 `exam.sections`，
  按 `section.partTitle || section.key` 从 `result.sectionScores` 取 `{correct,total}`，
  以 `section.key` 写入 `data.sectionHistory[key]`）、`getSectionHistory()`；
  `mergeData` 合并时走 `syncMergeSectionHistory(cur, imp)`（按 `sectionKey` 合并、
  按 `submittedAt|examId` 去重）。
- `js/exam.js`：`submit()` 交卷时调用 `Store.addSectionHistory(exam, {...result, submittedAt})`。
- `js/ui/dashboard.js`：能力概览六行改为从 `Store.getSectionHistory()` 按题型键聚合
  「累计对题 / 累计总题」，各自显示该题型历史正确率 + 累计次数（`N次`）；
  删去已变死代码的 `totalScore/pct` 计算。

### 2. 验证

- `.work/sectioncheck.html`（`SECTIONCHECK-DONE total=11 fail=0`）注入两套不同题型
  历史（reading 跨两套累计 17/20 → 85%、cloze 5/10 → 50%、grammar 3/10 → 30%，
  其余无历史 → 待开始），断言：六行各自聚合、不挂钩 `records` 数（records=0 仍显示 85%）、
  累计次数标注、六行不全相同。
- 37 路由 DOM 快照重取基线（`snap-section-after.json`）：与 `snap-after.json` 比对，
  **仅 `#/dashboard` 一处 diff**（能力概览 64%→待开始），其余 36 路由 IDENTICAL，
  无任何意外回归。注：`#/` 是首页（`reference-home`）不渲染能力概览，故两版均不含
  `ability-row`，属既有行为。

---

### 本轮续做（2026-08-23 晚）：代码层面优化

用户问“代码层面还有别的可优化的地方吗”，审查后落地四类优化（用户勾选全部四项）。

### 1. 健壮性
- `js/store.js`：`_save` 加 try/catch + 返回布尔，写入失败（隐私模式/配额满）降级为内存态、不抛错，避免冒泡到 `Exam.submit` 卡死答题页；新增进程内缓存 `_cache`，`_load` 返回浅拷贝不再反复 JSON.parse，`_save` 内部 `clearTimeout` 取消挂起防抖写、防止迟到写把刚提交的草稿写回。
- `js/exam.js`：`submit()` 整链包 try/catch，失败走 `App.renderError`（草稿仍在，刷新可接回）。
- `js/knowledge-graph.js`：`init()` 加 `resp.ok` 校验；`js/app.js` 的 `#/knowledge-graph` 路由加 `.catch` 兜白屏。

### 2. 性能
- `js/store.js`：`saveDraft` 改为内存态立即更新 + 写盘防抖 400ms（写作长文实时输入不再同步卡）。
- `js/training.js` / `js/topic.js`：试卷 fetch 由串行 `for…of await` 改 `Promise.all` 并行，进训练/专题页更快。
- `js/ui/dashboard.js`：合并两次 `_load` 为一次；`records.sort` 改 `.slice().sort` 避免原地污染缓存。

### 3. 公共工具去重
- `js/ui/base.js`：新增 `UI.countAnswered`（已作答统计）、`UI.eqAnswer`（答案相等判定）、`UI.referenceHeader`（顶栏骨架）。
- `js/store.js`：新增全局 `classifyKey`（错题归类唯一口径），`js/ui/mistakes.js` 的 `nodeOf` 改为别名引用，消除“改一处要改两处”。
- 散落 4 处 countAnswered、5 处 eqAnswer 判定、4 处 referenceHeader 拼装统一收口。

### 4. 刻意未做：选项渲染统一
exam-page 的可作答选项带 `data-qid` / `disabled` / `correct` 高亮（答题卡 + 解析模式独有），topic 的可作答选项没有这些，training-page 的 `lesson-option` 是只读展示（无 input）。三者语义不同，强行抽离会引入脆弱的条件拼接或改变输出字节，违背“等价重构”原则，保留各自实现更清晰。

### 5. 验证
- `node --check` 12 个改动文件全 OK。
- 37 路由 DOM 快照与分题型后基线 `snap-section-after.json` 比对：**全部 IDENTICAL**（零回归）。
- `.work/sectioncheck.html` 分题型断言 `SECTIONCHECK-DONE total=11 fail=0` 重跑通过（store 缓存/防抖重构安全）。
- 新增快照基线 `snap-opt2.json`。

---

## 二、未做的工作

### A. 代码任务全部完成

**#14 主观题判分自评表** ✅ 已完成（2026-08-22）

在 `Topic.renderWriting`（`js/topic.js`）里新增结构化自评区：
- 三档维度：要点覆盖 / 篇章连贯 / 语言准确（各「达标 / 基本达标 / 待加强」）
- 实际词数输入 + 自动从题干识别词数要求
- 备注栏
- 自评随草稿一起存进独立 localStorage 键（`gaokao_writing_draft_*`），下次打开自动恢复

短文改错逐项自评：在 `Topic.reportProofreading` 每处加「改对 / 漏改 / 改错」三选一，
汇总成 `改对 N · 漏改 N · 改错 N`，存进独立键 `gaokao_proof_self_*`。
重开时显示上次自评横幅。

护栏 `.work/selfcheck.html`（`SELFCHECK-DONE total=8 fail=0`）验证：
写作自评四维度+词数+备注持久化、重开自动恢复、空自评存 null、
逐项按钮渲染（每题 3 个）、逐项持久化、汇总文本、重开横幅、
存储键不走 `gkyy_records_v1`（不影响 #15 合并）。

**#15 跨设备进度导出/导入** —— ✅ 已完成，详见第一节 #4。

### B. 数据侧的真实缺口（不在批准清单里，但这是最大的短板）

| 项目 | 现状 |
|---|---|
| 试卷总数 | 16 套，`quality` 全部 `verified` |
| 客观题 | 955 道，全部带答案 |
| 听力原文 | **65 / 170** 有 transcript |
| 听力音频 | 只有 2021 的三个卷（`audio/` 下 3 个 mp3） |
| 整个听力部分缺失 | 6 套：gk2022-jia / gk2022-xgk1 / gk2022-yi / gk2023-jia / gk2024-xgk2 / gk2025-new2 |
| 完全没有听力原文 | 4 套：gk2024-jia / gk2024-xgk1 / gk2025-new1 / gk2026-xgk1 |

这一块是「先把听力放掉」的范围，本轮没碰。

另有一条来自 `todo.txt` 的待办：**2026 全国Ⅰ卷可以重录** ——
`tools/pipeline/raw_text/2026 Ⅰ.txt` 里 1–67 题答案完整（含听力 20 题答案），
只是正文单词被压缩掉了空格，需要逐段还原分词。这是人工活。

### C. 已明确放弃的

- 听力**功能**开发：音频播放器、原文补录。
- `quality` 字段的数据边界工作 —— 你说「数据边界已经审过了」。

---

## 三、接下来的方向

### 下一步方向：#14 主观题判分自评表（#15 已完成）

你原话的顺序是「主观题判分自评表是对的，跨设备是对的」，#14 在前。
但我倾向反过来，理由是两者的**失败代价**不同：

#15 解决的是一个会造成实际损失的问题。现在所有学习记录只活在一个浏览器的
localStorage 里 —— 清一次缓存、换一台电脑，错题本和复习排期全部归零。
而刚做完的间隔复习越有用，这个风险就越大：**排期是按天累积的资产，丢了没法
重建**（做过的题可以重做，但「这道题已经走到第 4 轮、下次 30 天后见」这个
状态是时间换来的，没有备份就是永久损失）。

#14 是能力增强。没有它，学生的主观题自评停留在「贴一段文字」；
丢了不心疼，晚一轮做也不损失既有数据。

如果你没有特别偏好，按你原来的顺序做 #14 也完全可以 —— 只是希望你知道
这个权衡再决定。

### 再往后

按依赖关系，#14 / #15 做完之后自然浮现的是这两件：

1. **听力补全**（数据活，不是代码活）。这是全站唯一的结构性短板：
   六套卷根本没有听力部分，学生练不了。补原文需要音频转写或找到源档，
   补音频需要版权可用的录音 —— 这两件都不是「写代码」能解决的。
2. **分题型得分数据** ✅ 已完成（2026-08-23，见「本轮续做」一节）。仪表盘「能力概览」
   六行改为按题型读 `Store.getSectionHistory()` 聚合历史正确率，不再共用单一百分比。

---

## 四、需要你决定的三件事

这三件已在本次会话开头处理完毕（你已点头）：①清理 `.work/` 里 631M Chrome 临时
profile 目录、②删除损坏副本 `.work/audit.html`、③#15 跨设备导出/导入优先做。
下面是当时留下来的记录。

### 1. `.work/` 里 631M 是垃圾，清不清

`.work/` 总共 647M，其中 **65 个 Chrome 临时 profile 目录**（`cdpA`、`cdpB1`、
`cdpRV3` …）占 631M。跑一次无头测试生成一个，纯垃圾。

```bash
rm -rf .work/cdp*
```

不影响任何护栏和基线文件（`snap-before.json` / `topic-before.json` /
`draft-before.json` 都不在里面）。

其余可考虑清理的：`.work/tooling/`（4M，prettier + acorn 的临时安装）、
`.work/out-*.html` 与 `.work/smoke-out.html`（护栏输出的临时转储）、
`.work/oldpair/`（旧 `render.js` 的副本，28K）、`.work/extract/`（10M）。
两个备份目录 `.work/backup-refactor-20260822/`、`.work/backup-design-20260822/`
各 100K 左右，留着不占地方，你决定是否保留。

### 2. `.work/audit.html` 是坏文件，需要你指名删除

它是根目录 `contrast-audit.html` 的损坏副本：`<div id="out">` 里**烤死了**
结果字符串（`AUDIT-DONE routes=22 violations=0 emptyRoutes=none`），
而脚本第 117 行本该是结果拼装表达式的位置，留着的是字面量 `'RUNNING`。
跑起来必然报 `HARNESS-ERROR ... is not valid JSON`。

我尝试删除时被权限判定拦下，原话是「需要用户明确指名 `.work/audit.html`
才放行」。留着的风险是日后误跑它、看到报错以为真出了问题。
能用的那个是根目录的 `contrast-audit.html`。

### 3. 选方向

- **#14 主观题判分自评表** —— 你原定的下一个
- **#15 跨设备导出/导入** —— 我建议的下一个（见第三节的理由）
- **2026 卷重录 / 听力补全** —— 人工活，需要你确认愿意投入这个时间

---

## 五、断点位置

本轮结束时：#13 间隔复习、#15 跨设备导出/导入、#14 主观题判分自评表、
**分题型得分数据** 全部完成、已验证、已写进 `overview.md`，任务表里全部标 completed。

数据侧补全（听力）仍是从「数据侧补全」开始的方向（见第二节 B）。
工作区是干净的：全部护栏绿、37 路由快照基线已更新到最新状态（仅 #/dashboard 预期 diff）。

---

## 六、相关文件索引

| 文件 | 内容 |
|---|---|
| `overview.md` | 逐轮技术记录（追加式，每轮一节：问题 / 处理 / 验证 / 边界与说明 / 修改文件） |
| `task.md` | 产品路线图与已完成能力清单 |
| `todo.txt` | 题库录入进度与源档瑕疵记录 |
| `题库待补清单.md` | 逐卷逐题的数据缺口 |
| `DESIGN_OPTIMIZATION.md` | 本轮工作的起点（设计审计报告） |
| `.work/*.html` | 八个护栏（快照 / 对比度 / 可访问性 / 视觉 / 错因分类 / 间隔复习） |
| `.work/*.py` | 快照存取与比对脚本 |

---

## 七、今日小结（2026-08-31）：词组闪卡 / 朗读 / 知识库 A 块

> 本轮做到一半收工。已完成的部分全部验证过；未完成的部分在下面逐条详述，
> 下一轮从「未完成 1」直接接。

### 已完成（按顺序，均已验证）

1. **词组星图（第 6 张图，`phr` 橙色系）** ✅
   `js/ui/synonyms.js` 六图 + `data/vocab/gaokao-phrases.js`（4,487 条，
   修正了 index.js 的 file 字段与 id 不一致的 404 bug）+ CSS 橙色主题。
   验证：45 路由快照 44 处 byte-identical + `phr_probe` 14 PASS。
   数据口径记录在 `data/vocab/SOURCES.md`。

2. **541 条无释义词组 → 备查底稿** ✅
   `data/vocab/phrases-no-meaning.md`（按 6 个源文件分组，`tools/dump_no_meaning_phrases.py` 生成）。

3. **词组闪卡（#/phrases）** ✅
   `js/store.js` 新增 phrases 数据区（addPhrase / removePhrase / getPhrases /
   reviewPhrase，排期复用 Review 1/3/7/15/30 阶梯，跨设备合并复用
   syncMergeWords 同口径）；新文件 `js/ui/phrases.js`（翻卡：正面词组+朗读、
   背面释义+例句+认识/忘记；「随机收录 10 条」只收有释义的；冷启动引导）；
   `js/app.js` 路由 + phr-* 动作；`index.html` 引入 phrases.js；
   `css/platform.css` 闪卡样式；星图词组详情卡新增「收进闪卡」与数据互通。
   验证：`node --check` 全过；`.work/flash_probe.html` 探针 **22/22 PASS**
   （收录/排期/翻面/判定/持久化/朗读能力检测/星图互通/缓存 4487）。

4. **朗读（浏览器 SpeechSynthesis）** ✅
   `js/ui/base.js` 新增 speakSupported / speakBtn / speak（无 API 时按钮不渲染，
   护栏快照确定性不受影响）；挂载点：词条页 h2、词书词条例句行、生词本到期行、
   词组闪卡卡面、星图词组卡。`js/app.js` 新增 `'speak'` 动作。
   真题听力音频的版权缺口仍由人工清单跟进，此功能是零成本的兜底。

5. **A5 知识节点 38 → 50** ✅
   `tools/build_knowledge.py` 新增 12 节点（语法·句法 7：情态动词表推测 /
   need与dare的用法 / 主将从现 / 定语从句that与which的选择 / 介词+关系代词 /
   as引导的定语从句 / 情态动词+have done；语法·词法 4：不定代词 / 指代词辨析 /
   倍数表达法 / so与such的区别；词汇·用法 1：动词接续（doing与to do））。
   分类分布：句法 25 / 词法 13 / 词汇 8 / 写作 4。

6. **A6 strength 标注 + commonErrors 三段拆分** ✅
   全部 50 节点加 strength（rule 44 / tendency 1 / register 5，诚实标注）；
   commonErrors 全量改为 wrong / right / why 三段（旧键 error/detail 零残留）；
   check_structure / check_content 同步改造；构建一次通过，
   `data/knowledge/index.js` 已重建（node --check 过）。

7. **A8 节点级题池 400 题，50 节点全覆盖** ✅
   `tools/kb_questions_a.py`(128) / `_b.py`(104) / `_c.py`(128)（三个并行子代理
   产出）+ `tools/kb_questions_d.py`(40，手写补写作 4 节点 + 动词接续)。
   `build_knowledge.py` 新增题池管线（QUESTION_BATCHES 加载 → check_questions
   校验 → 输出 `data/knowledge/questions.js`，含每节点变体清单）。
   校验：`.work/check_bank.py` total 400 / nodes 50 / 每节点恰 8 / fill ≥2 /
   节点名与 KB 全对齐。构建输出「题池： 400 题」。
   ⚠️ **所有题目 verified=False**：机器自编未人工复核，前端必须灰标展示
   （待 A9/A10 前端实现时执行），复核后方可改 True。

### 未完成（下一轮从 1 接，详述）

1. **A9/A10 前端：题池练习 + 五级台阶判级 + 间隔复练联动 —— 未开始，最优先**
   - ①（先做，属兼容性修复）`js/ui/mistakes.js` 约 283–284 行仍读旧结构
     `e.error / e.detail`，而 A6 已把 commonErrors 改为 wrong/right/why ——
     **当前知识详情页「常见错误」区渲染必然是 undefined**。改为三段渲染
     （wrong 划掉 / right 正确句 / why 原因）。
   - ② `knowledgeDetail` 新增「节点练测」区：惰加载
     `data/knowledge/questions.js`（script 注入，参照 data-loader.js / synonyms.js
     `_phraseReady` 模式），练习流：choice 点选项即判、fill 输入回车判，
     每题答完出解析；verified=false 的题带灰标「未复核」。
   - ③ `js/store.js` 新增 knowledgeProgress 区：
     `{ [nodeId]: { seen, correct, variants:{v:{seen,correct}}, reviewStage, nextReviewAt } }`；
     `saveKBAnswer(nodeId, variant, correct)` 与 `reviewKBNode(nodeId, passed)`；
     跨设备合并新增 syncMergeKBProgress（口径：台阶更靠前者胜，同阶取
     seen/correct 较大者，仿 syncMergeWords）。
   - ④ 判级（task.md §8.3 口径）：还没开始（未做题）→ 有点模糊（<40%）→
     有点思路（≥40%）→ 基本会了（≥70% 且每变体至少对一题）→ 挺熟了
     （100% 且走完 1/3/7 天三轮间隔复练）。到 100% 自动排 1/3/7 复练，
     复练全对 stage+1，出错 stage 归零重算。
   - ⑤ `#/learn` 节点卡片加 tier 徽标；`mistakes.js` 顶部 NODE_RULES
     （22 条兜底规则）确认 50 节点全覆盖后可退役删除。
   - ⑥ 新探针：判级边界（0/40/70/100%）、变体覆盖、复练推进与归零。

2. **A7 tools/add_question.py —— 未开始**
   CLI 录题工具：--node/--variant/--kind/--stem/--options/--answer/--explain
   必填、--source 必填（拒绝空来源）、verified 默认 False；校验 node 存在、
   choice 四选一答案在 ABCD、fill 答案非空后追加到
   `tools/kb_questions_custom.py`（build 的 QUESTION_BATCHES 已预留该名字，
   build 无需再改），最后提示重跑 build_knowledge.py。

3. **F4 考点/词频透视（#/insights）—— 未开始**
   纯前端聚合：16 套卷 955 道客观题按 Diagnose 规则归类出节点频次排行、
   按年份分布、完形选项高频词。注意确定性渲染（它是快照路由）。

4. **F5 错题混排重练（#/drill）—— 未开始**
   从错题本抽 10 道可判分客观题（有 options 且有 answer），打乱逐题作答
   当场判分；答对推进间隔复习（走 Store.markMistakeReviewed，
   注意其 index 是「从末尾数」的口径：idx = items.length - 1 - position），
   答错保持原状；结束时给小结。

5. **F6 打卡日历 / streak —— 未开始**
   dashboard 加近 17 周 GitHub 风格热力图 + 连续天数。活动数据源：
   records.submittedAt、mistakes 时间戳、topicRecords、words/phrases 的
   addedAt 与复习动作。注意 #/dashboard 快照基线会变（预期 diff）。

6. **F9 体验层小件 —— 未开始**
   全站搜索（#/search：卷标题/年份/知识节点/词书词条/词组，纯前端）；
   键盘快捷键（答题页 A–D 选项、复习队列 1/2/3、闪卡空格翻面；
   输入框聚焦时忽略）；PWA manifest（file:// 下 SW 不可用，先加
   manifest.webmanifest + meta，SW 留待正式部署）；深色模式：CSS 硬编码
   颜色多、成本高，**建议单独立项**，本轮未写入 8.8。

7. **其余脑洞写入 task.md §8.8 —— 未做**
   三项待记：无释义词组补录工作流（把 541 条底稿做成可勾选进度的小工具）、
   目标分预测（只做事实呈现不做玄学）、打印/PDF 导出（错题本/生词本/复习队列）。

8. **总验证收尾 —— 未做**
   已过：改动 js 全部 node --check；flash 探针 22/22；index.js 与
   questions.js node --check；build 五类一致性检查 + 题池校验。
   未做：① domdump.html 路由表加 #/phrases；② 45 路由快照**重设基线**
   （本轮已知预期 diff：#/synonyms 词组卡多两个按钮、#/word 加 ♪读、
   #/words 到期行加 ♪读、词书首页文案一句、#/phrases 新路由；基线不更新
   前这些都会被记为 diff）；③ A 块前端落地后再补判级/复练探针并重取基线。

9. **新增人工待办（可记入人工待办清单）**
   - 400 题题池人工复核（复核一题把 verified 改 True；建议按句法 25 /
     词法 13 / 词汇 8 / 写作 4 个节点分类分批）；
   - 12 个新知识节点内容审校（并入清单第一节批次 5）；
   - 「收进闪卡」与词组数据在发布前的整体过目（人工待办清单第五节）。

### 本轮改动文件清单

`js/store.js`、`js/app.js`、`js/ui/base.js`、`js/ui/word.js`、
`js/ui/synonyms.js`、`js/ui/phrases.js`（新）、`index.html`、
`css/platform.css`、`tools/build_knowledge.py`、`data/knowledge/index.js`（重建）、
`data/knowledge/questions.js`（新）、`tools/kb_questions_a/b/c/d.py`（新）、
`.work/flash_probe.html`（新）、`.work/check_bank.py`、`.work/list_nodes.py`（临时）。

---

## 八、今日小结（2026-09-01）：A 块收尾 + F 系列体验层全部落地

> 昨日 §七 未完成清单 1–8 全部完成并验证。task.md §8.8 的 A5–A10、F4/F5/F6/F9
> 已勾销，剩余 G1–G3（补录工作流 / 目标分呈现 / 打印导出）与 A11 留档。

### 已完成（按昨日未完成清单序号对应）

1. **坏档修复 + A9/A10 前端（昨日 1）** ✅
   常见错误三段渲染（wrong 划掉 / right 改正 / why 原因）+ 强度徽标；
   节点练测区全量上线：题池惰加载、盒子级重渲染（作答不跳顶）、choice/fill
   两版式、变体命中统计；Store.knowledgeProgress 数据层
   （saveKBAnswer / reviewKBNode / syncMergeKBProgress）；五级判级
   （还没开始→有点模糊→有点思路→基本会了→挺熟了）、100%+全变体命中自动
   排 1/3/7 三轮复练（全对 stage+1、走完毕业、出错归零）、复练验收不计入
   练测进度；#/learn 台阶徽标。修了两个真 bug：会话 stale 导致复练入口
   不出现（kbQuizShell 每次整页渲染清 _kbq）、复练误计进度。
   验证：`.work/kbq_probe.html` **21/21 PASS**（今天补了状态清零，防 profile 残留）。

2. **A7 录题工具（昨日 2）** ✅
   `tools/add_question.py`：node/variant/kind/stem/answer/source 强校验、
   同题去重、写 `tools/kb_questions_custom.py` 后提示重跑 build。
   冒烟：正例入库 + 坏节点/缺来源/fill 带选项 三负例拒绝 + 重复题拒绝；
   顺带修了 dump_custom 把 JSON 的 false 写进 Python 文件的 NameError bug。

3. **F4 考点/词频透视 #/insights（昨日 3）** ✅
   16 套卷 795 道客观题跑 Diagnose 规则的静态聚合：考点频率榜 TOP24、
   题型×考点各 Top5、完形高频词（出场次数+被设为正确答案的次数，链接词条页）。
   数据不掺学生记录，同一天两次刷新一致（确定性，可进快照）。
   验证：`ins_probe` 16/16（修了 cloze options 是 {letter,text} 对象的取词 bug）。

4. **F5 错题混排重练 #/drill（昨日 4）** ✅
   抽 10 道题干完整的客观题打乱重做；可判分口径刻意收紧：只收
   阅读/听力（完形/语法题干是「第41空」残句，离原文没法重做），选项按
   examId+qid 回题库现查（错题白名单没存 options）。答对 → 复习阶梯+1
   （markMistakeReviewed，倒序下标已处理）、答错不动排期；结尾小结可再抽。
   验证：`drill_probe` 16/16。

5. **F6 打卡日历（昨日 5）** ✅
   dashboard 17 周 GitHub 风格热力格 + 连续打卡天数（今天未学不打断）。
   活动口径五路时间戳：整卷 submittedAt / 专题 completedAt / 错题 ts /
   生词与词组 addedAt。四档深浅，空库有引导文案。
   验证：`hm_probe` 11/11（修了 records 是按键对象不是数组的遍历 bug）。

6. **F9 全站搜索 / 快捷键 / PWA（昨日 6）** ✅
   #/search 四路客户端匹配（试卷/知识节点/单词/词组），200ms 节流、
   只刷结果区不丢焦点；快捷键：考试页 A–D 选第一个未作答题、复习队列与
   错题详情 1/2/3 改状态、闪卡空格翻面、/ 跳搜索（输入聚焦时全部让路）；
   manifest.json + assets/icon.svg + theme-color（file:// 下浏览器自动忽略，
   部署后可安装；Service Worker 刻意不做；深色模式建议另立项目）。
   验证：`f9_probe` 14/14。

7. **总验证收尾（昨日 8）+ task.md 勾销（昨日 7）** ✅
   - 14 个改动 js 全部 node --check 通过；
   - 旧探针回归：flash 22/22、kbq 21/21（发现并修复探针被 A7 测试题污染
     定语从句节点 8→9 题的问题：custom 批次重置为空、题池回到 400 题）；
   - domdump.html 路由表扩为 **50 条**（新增 #/knowledge/定语从句、
     #/phrases、#/insights、#/drill、#/search）；
   - 快照重设基线：45 条共同路由 8 处 diff 全部为预期（#/ 导航加两入口、
     #/dashboard 插入打卡日历、#/knowledge 与 #/learn 与知识星图 38→50、
     #/knowledge/主谓一致 加强度徽标、#/word 两处朗读相关）；
     新基线 **`.work/snap-f-post.json`**（snap-rev.json 同步更新），
     旧基线归档 `.work/snap-phr-pre-f.json`；
   - task.md §8.8：A5–A10、E2、F1/F2/F4/F5/F6/F9 勾销，新增
     G1 无释义词组补录工作流 / G2 目标分区间呈现 / G3 打印导出；
     §九追加第 16 条执行记录。

### 遗留（已入 task.md §8.8，机器侧暂缓）

G1 补录工作流、G2 目标分区间、G3 打印导出、A11 单元 mini 诊断；
人工侧：400 题题池复核（verified 改 True）仍是最有价值的下一步。

### 本轮改动文件清单

`js/ui/mistakes.js`（练测区+判级+三段渲染）、`js/store.js`（knowledgeProgress）、
`js/review.js`（kbTier/KBQ_STAGES）、`js/app.js`（路由+动作+hotkey+搜索 input）、
`js/ui/insights.js`（新）、`js/ui/drill.js`（新）、`js/ui/search.js`（新）、
`js/ui/dashboard.js`（打卡日历）、`js/ui/library.js`（导航两入口）、
`index.html`（3 个新脚本 + manifest/theme-color）、`css/platform.css`（五段新样式）、
`manifest.json`（新）、`assets/icon.svg`（新）、
`tools/add_question.py`（新）、`tools/kb_questions_custom.py`（空批次）、
`data/knowledge/questions.js`（重建）、
`.work/kbq_probe.html`、`.work/ins_probe.html`（新）、`.work/drill_probe.html`（新）、
`.work/hm_probe.html`（新）、`.work/f9_probe.html`（新）、`.work/domdump.html`（50 路由）。

---

## 九、项目状态整理 + 用户视角不足分析（2026-09-01 晚）

### A. 仓库清理（.work 155MB→22.8MB，根目录归档）

删除项（全部可再生或已被现行基线取代）：

- **Chrome 探针缓存目录**：.work/cdp* 全部 20 个（约 92MB）——headless
  profile 每次跑探针自动重建，且探针已内置程序化清态，不依赖残留；
- **一次性诊断产物**：`_*` 临时 dump/txt/py（38 个：hexctx/scroll/overflow/
  token 诊断等历史排障文件）、`*_dump.html` 快照输出、`out-*.html`、
  `draft-*.json`、旧快照基线 41 个（44→3）；
- `.work/__pycache__`、过期 cmp/cmpsnap/savetopic 重复脚本。

保留项（护栏与追溯）：

- 现行基线 **snap-f-post.json** + 同步的 **snap-rev.json** + 归档参照
  **snap-phr-pre-f.json**；快照工具链 cmp_snap.py / savedump.py /
  savesnap.py / probe_out.py / domdump.html（50 路由）完整；
- **13 个护栏探针**全数保留（kbq/ins/drill/hm/f9/flash/syn/word/kg/
  subskill/sectioncheck/listencheck 等）+ smoke-test.html + contrast-audit.html
  + a11y/visual 审计页；
- 历史备份目录（backup_exams_20260825 / backup_knowledge_20260829 /
  backup-kg-20260830 等）按「退役文件备份保留」铁律不动；
- 根目录一次性报告（短文改错答案核验报告 / 题库路径漂移核查 / todo.txt）
  移入 **归档报告/**（未删，可追溯）；DESIGN.md（设计锚）、DESIGN_PLAN/
  DESIGN_OPTIMIZATION（标注为历史决策记录）、人工待办清单引用的
  待人工审查清单/题库人工审稿报告均保留原地。

### B. 以用户视角看项目不足（高三学生走查）

> 换位成一个每天晚自习后打开这个网站的高三学生，按「打开→练习→复盘→
> 重练→追踪」的真实旅程走一遍，缺口按痛感排序：

1. **听力是半残的**。16 套卷只有 2021 年 3 套有音频（audio/ 3 个 mp3），
   其余年份听力题只能看题干凭空作答——学生第一次做 2023 卷听力就会
   撞上「音频呢？」。这是全站最大的信任硬伤（听力资源清单已记录缺口）。
2. **「我现在多少分」没有答案**。学生打开网站的终极问题是目标分差距，
   G2 分数区间呈现还没做；dashboard 有六行分题型正确率和打卡日历，
   但没有一个「按当前正确率折算 ≈ XX–XX 分」的结论性数字。
3. **最想重练的题型恰恰重练不了**。drill 只收阅读/听力，完形和语法填空
   的错题因为题干是残句进不了池——而这两类恰是错误率最高的。学生
   复盘完完形错题，点「去重练」却发现自己的错题不在池里。
4. **学了知识点，但不知道整体水平**。50 节点都有判级和复练，但没有
   单元级 mini 诊断（A11）——学生无法回答「我语法到底行不行」。
5. **写作几乎没有反馈**。应用文/续写是纯自评四维度 + 词数，没有范文
   对照批注、没有任何外部反馈机制，写完和没写的区别只有一个草稿。
6. **数据是 localStorage 单点**。换电脑、清缓存、重装浏览器 = 全部
   学习记录清零。#/data 有导出导入，但没有任何「该备份了」的主动提醒，
   学生不会知道要防这一天。
7. **新用户没有起点**。空状态文案齐全，但没有 onboarding：不定水平、
   不定目标、不生成「今天该做什么」的计划——一切都靠学生自己逛出来。
   复习队列承担了一部分，但它只覆盖错题和生词。
8. **手机场景悬空**。整套站未部署，PWA/manifest 是备而不用的；而高三
   学生的碎片时间几乎全在手机上，桌面 file:// 的假设本身就是开发者视角。
9. **内容规模与可信度还撑不起「刷透真题」的承诺**。真题 16 套（2021–2026）、
   题池 400 题带「未复核」灰标、541 条词组无释义、12 个节点内容待审校——
   学生刷到灰标题、查词组查不到释义时，信任是往下走的。
10. **夜间刷题刺眼**。深色模式因 CSS 硬编码色值暂缓——晚自习后的
    主要使用时段恰好是最需要它的时候。

> 一句话总结：功能骨架已经全了（练测/复盘/重练/追踪/查词/搜索闭环），
> 但「内容完整性（听力音频+题池复核）」「结论性反馈（分数区间/整体水平/
> 写作批改）」「数据安全（备份提醒）」「触达（部署+手机）」四件事
> 决定它从「能用的作品」到「敢推荐给学生用」的距离。

### C. 本轮无代码改动，护栏未触发

清理只动 .work 临时产物与根目录文档归档，js/css/data/index.html 零改动，
快照基线与探针不受影响。

---

## 十、知识星图 v2：四张可旋转的球面星图（2026-09-01 深夜）

> 用户定稿：原 50 星一张大平图「太紧太密没有意义」；要四张有旋转空间感的
> 星图，点星能亮起呼应的知识点；太多就分四栏各自进入。本轮照此重做。

### 交付

- **门厅四栏**（#/knowledge-graph）：句法 25 / 词法 13 / 词汇 8 / 写作 4，
  每栏带类别色、一句话介绍、前瞻节点芯片与「N 条类内关联 · M 条跨类」。
- **单类球面星图**（#/knowledge-graph/<类别>）：斐波那契球布点，深度决定
  星的大小/亮度/标签浓淡（近亮远暗，painter 次序渲染）；**拖拽旋转**
  （pointer 委托挂 App.init，move/up 挂 window 防出框；拖拽中只按 data-*
  基坐标重投影、不走 innerHTML）；**自转开关**（默认关）。
- **点星唤醒**：球体把该星缓动（480ms ease-out）转到正面，它与 related
  节点一起亮起、链线点亮（亮 0.9 / 灭 0.05 / 无关星 0.16），知识卡给
  规则、自评掌握度、关联清单；**跨类关联一键跳**到另一张星图并唤醒对应星。
- 快照安全：球面基坐标（斐波那契球）与默认姿态（rot -0.35/0.55）固定，
  同数据渲染逐字节一致；动画只改 DOM 属性。

### 改动文件

`js/knowledge-graph.js`（整体重写）、`js/app.js`（新 PATTERN 路由
#/knowledge-graph/:cat、kg-star 增拖拽抑制与 faceTo、kg-jump、kg-spin、
pointer 三监听）、`css/platform.css`（门厅四栏 + 星空工具条 + 关联芯片样式）、
`.work/kg_probe.html`（重写）、`.work/domdump.html`（51 路由）。

### 护栏

- kg_probe 重写 **29/29**：门厅计数/链接、星数/链线与数据一致、深度感、
  点星唤醒（亮链/灭链/变暗/转到正面偏移 0,0）、拖拽旋转（rot.y 精确 +0.6、
  拖拽后合成点击被抑制、正常点击仍唤醒）、自转开关推进、跨类跳转、熄灭回全图。
- 51 路由快照：50 条共同路由仅 #/knowledge-graph 一处预期 diff，基线
  **snap-f-post.json** 重设（snap-rev.json 同步）。
- 顺带发现并修掉两支**昨日未回归的过时探针**（页面无恙、断言过时）：
  syn_probe 56/56（F2 给 #/synonyms 加了第 6 个「词组」页签）、
  word_probe 42/42（F1 的 ♪读 按钮进了 .word-hw 的 textContent，
  等值断言改 includes）。教训入记忆：**新增 UI 元素进旧选择器的
  textContent 时，当天必须回归所有相关探针**。
- 探针环境备注：headless 虚拟时间下 rAF 不随真实时间泵帧（动画补间被攒批
  跳变），探针开头垫 `window.requestAnimationFrame = cb => setTimeout(...)`；
  iframe 内的指针事件必须派发到 iframe 的 window，不能发到探针父页。

### 遗留

- 星图数据层零改动（仍用 related[] 派生边）；边带类型（先修/易混/共现）
  的彩色区分维持旧图退役时的取舍，未随 v2 恢复。
- 移动端触摸旋转走 pointer 事件已可用（touch-action:none），真机未实测。





## 十一、星图集 v3：单词星图对齐知识星图形态（2026-09-01）

参考 ai-knowledge-graph（robert-mcdermott）的 degrees=2 邻域高亮思路落地：
一阶全亮 + 二阶半亮 + 细虚线经一阶中转，不做力导向/社区发现/运行时 LLM。
按用户要求把单词星图（#/synonyms）改成与知识星图 v2 一致的「可旋转空间感、
点击一颗星唤醒呼应知识点」的形态。

### 形态

- **门厅**（#/synonyms）：六栏星图入口（同义/近义/反义/前缀/后缀/词组，
  各带 --sync 关系色、当前中心词的真实星数、一句话说明、进入星图链接）+
  探索框 + 统计条（词族/词缀/星图，只用同步数据保证快照确定性）+ 词缀表 +
  全部词族（原有速查功能不动）。
- **单图页**（#/synonyms/<map>，新增 PATTERN 路由）：SVG 球面星空
  （斐波那契球布点 R=208、viewBox 900×560），中心词以 faceRot 固定转正面
  （渲染即落位 450,272，确定性），一阶环星 ≤14 + 词缀门户星 + 二阶关联 ≤10
  （仅 syn/near/ant、仅词中心）按深度决定大小/亮度/标签浓淡；页签切换保持
  中心词；拖拽旋转（pointer 委托与 KG 同套：move 阈 4px、rot.x clamp ±1.25、
  拖后合成点击抑制）只做重投影不走 innerHTML（快照安全）；自转开关 rAF
  +0.003/帧，离开页面自动停。v1 语义全保留：词缀中心词义页签灰置、家族词
  回词中心保持所在图、详情卡随图裁剪、空图一键跳转、查词条互通、词组卡
  phr-toggle 与闪卡互通。
- **跨页唤醒**：门厅探索框、词条页 word-syn 芯片、门厅词缀芯片提交后由
  synCenter 统一走「惰索引就绪 → 判中心类型 → 不在单图页则 _synAutoMap +
  location.hash 跳 #/synonyms/<map>」，word-syn 不再抢先改 hash（消除与惰
  加载的赛跑）。

### 改动

`js/ui/synonyms.js`（SYN_MAP_COLOR/SYN_SKY 常量、synonyms() 改门厅、新增
synPage()/synCenter→_synCenterGo 拆分/球面 _renderConstellation/_synSecond/
_skyProject/_skyReproject/drag 三件套/setSynSpin）、`js/app.js`（PATTERN 路由
#/synonyms/(syn|near|ant|pre|suf|phr)、syn-star 拖拽抑制、syn-spin、
pointer 委托扩展 [data-syn-sky]、word-syn 简化）、`css/synonyms.css`
（.syn-gate 六栏、.syn-sky 球面容器、.syn-snode 深度文本、.syn-line-g2
二阶虚线、响应式）、`.work/syn_probe.html`（重写）、`.work/domdump.html`
（52 路由，synonyms 路由 sleep 2600ms 等惰索引二次重绘落定）。

### 护栏

- syn_probe 重写 **68/68**：数据层回归、门厅六栏（计数 = 各图真实星数）、
  探索框重定向、hub 转正面（450.0,272.0）、深度感、二阶星（refuse 词 5 颗
  半亮 + 经一阶中转的细虚线）、点星唤醒/切图保中心、词缀门户（under- 进
  家族 + 页签灰置 + 家族词回词保持原图）、空图跳转、详情卡裁剪与查词条、
  拖拽（rot.y +0.6、拖后点击抑制、正常点击仍唤醒）、自转开关、词组图
  （get 14 颗 + above all 重定向 + 闪卡通畅）、近义辨析。
- 回归：flash 22/22（sleep 1500 适配异步重定向）、word 42/42（word-syn 断言
  改 #/synonyms/syn + hub）、kg 29/29 不受影响。
- 52 路由快照：共同 51 路由仅 #/synonyms 一处预期 diff；两次 dump 逐字节
  一致后基线 **snap-f-post.json** 重设（snap-rev.json 同步）。
- 截图目检：syn-gate.png（六栏+统计）、syn-sky.png（hub 居前 + 环星深度）
  均符合设计。
- 过程中修掉两个自伤 bug：_synCenterGo 重定向引用了未声明的 st（rejection
  捕获法定位：探针垫 unhandledrejection 监听）；探针两处 #syn-gate 断言
  未考虑 a11y 层会把首个 main 的 id 重写为 main（改断言而非改产品）。

### 遗留

- 移动端真机未实测（touch-action:none 已就位）。
- 二阶关联目前只走当前关系同类型扩展（syn 图二阶还是 syn），跨关系二阶
  未做（数据量会失控，暂不做）。
- KG v2 的 faceTo 补间动画（点星缓动转正面）未同步到星图集：单词语义是
  「换中心重渲染」而非「高亮邻域」，暂无补间诉求。

## 十二、星图集第七图：形近（2026-09-02 凌晨）

用户点题：universe/universal/university 同核家族、converse/conserve 易混对
——「把整个星图全联系起来」。形近是第七种关系（粉 #f0a3c2，单环），
与前六张互补：同义/近义/反义管词义，前缀/后缀管构词，词组管搭配，
形近管「长得像、容易看错记混」。

### 数据口径（铁律：不编造关系，纯词形计算）

- 词源 = **全部 8 本词书并集**（7127 词，4 字母起）。conserve 只在
  cet6/gaokao-michael、converge 只在词书有——高中单本根本连不起来，
  所以并集是必需而非优化。
- 建对条件：编辑距离 ≤2（<5 字母短词只认 ≤1）或共享 ≥6 字母词形核。
  命中 quite/quiet、accept/except、converge/conserve、than/then、
  universe/universal/university 全部经典易混对。
- 每词邻居按（距离→字典序）截 **10** 个：8 名额会把 quite/quiet 这种
  d2 同距对按字典序挤出去（quiet 前面排着 guilt/quaint/quart/queen/
  queer/quest），10 才保得住。
- ~2500 万对分片异步构建：700 词/片 setTimeout 让出主线程；构建期间
  _formIdx 保持 null（页签/星座自然显示 0 星），排序截断完成后一次性
  赋值并兑现 _formReady 的回调（Promise 全链路等真实完成）。实测 1.27s
  （formbench 探针）。曾试 26 位字母签名预过滤（2.4s→0.7s），但会漏
  letter/better 这类重字母 d1 对——正确性优先，弃用。
- **converse 词库未收 → 不造假入图**（探针有断言）；日后收进词书自动入图。

### 接入点

门厅第七栏 + 统计条 7 星图；单图页签（词缀中心时灰置扩到 sim）；
球面环星/中心连线/详情卡形近块（全量芯片 + 口径说明）；词条页关系卡
形近芯片（word.js 等 _formReady 后回填）。**synCenter 收录校验放行
_formIdx 里的词**——形近环上的 converge/refugee 只在词书、无词条，
原校验会把点击当「词库未收录」拒掉，形近图会半死；详情卡对无词条词
本就有空数组兜底，安全放行。

### 护栏

- syn_probe **80/80**：原 75 项 + conserve→converge/observe、converse
  缺席不造假、quite/quiet 对称断言（单向往 Quite 的 10 名额里可能被
  挤掉，双向任一命中即算连上）。
- 回归：flash 22/22、word 42/42、kg 29/29、drill 16/16、**phr 13/13**
  （旧探针还在用 v2 的 .syn-star-phr/.syn-node-center 选择器，修到 v3
  口径：先落 #/synonyms/phr 单图页再 synCenter，星读 g.syn-snode-ring，
  中心读 g.syn-snode-hub data-nid）。
- 53 路由快照（+#/synonyms/sim；synonyms 路由 sleep 2600→6000 等 8 本
  词书惰加载 + 分片构建落定）：两次 dump 逐字节一致；共同 52 路由
  4 处 diff 全预期——门厅七栏、syn 页签插 sim、word/happy 加形近块、
  dashboard 热力图日期滚动（环境噪声非代码）。基线 snap-f-post.json
  重设（snap-rev.json 同步）。
- 截图目检：syn-gate.png（七栏，形近粉卡）+ syn-sim.png（quiet 中心
  10 颗一阶粉星 quite/quilt/quiz/quest… + 10 颗半亮二阶）符合设计。

### 遗留

- converse 不在词书（八本都没有），图上确实没有它——如实呈现，不造假。
- 形近图二阶关联已生效（_synSecond 的 sim 分支），跨关系二阶仍不做。
- sim 门厅计数对默认中心 big 恒为 0（big 3 字母不进索引），与
  前缀/后缀/词组栏为 0 的语义一致（「颗星 · 中心 big」），未特判。
