# 星图集（#/synonyms）—— 五张独立星图

把词库里零散的单词整理成**以词为中心、彼此牵连的词族网络**：输入一个词，展开与它相连的一串词，像星星一样彼此牵连；点任意星星可换它做中心，每颗星都有专属详情界面。

> **2026-08-31 升级：一张混合图 → 五张独立星图。**
> 原来同义 / 近义 / 反义 / 词缀四种关系挤在同一张图上，颜色混杂、关系语义互相打架。
> 现在拆成**同义 / 近义 / 反义 / 前缀 / 后缀**五张图，一张图只讲一种关系，页签切换，
> **中心词跨图保持**。数据层一字未动。

## 五张图分别是什么

| 图 | 星从哪来 | 颜色 | 布局 |
|---|---|---|---|
| 同义 | 该词的同义词 | 绿 | 整圈 |
| 近义 | 该词的近义词（详情卡给一对一辨析） | 蓝 | 整圈 |
| 反义 | 该词的反义词 | 红 | 整圈 |
| 前缀 | 加前缀派生的词 + 本词的前缀门户星 | 紫 | **左翼弧** |
| 后缀 | 加后缀派生的词 + 本词的后缀门户星 | 青 | **右翼弧** |

- **页签带真实星数**：`同义(5) 近义(2) 反义(1) 前缀(0) 后缀(0)`。数量与图上真有的星严格一致（页签与星座渲染共用同一个 `_synStars(map)`）。
- **切图不动中心词**、不清搜索框、不重渲染下方词族列表——你正看着的那个词始终在中心。
- **空图不让人卡住**：显示「「big」在「后缀」图里还没有收录的词」+ 一颗按钮直接跳到有内容的那张图。
- **词缀门户星**（前缀/后缀图中心近旁那颗，如 `un-`、`-ment`）：点它换词缀做中心，展开整个词缀家族。
- **词缀做中心时**：三个词义页签灰置（词缀没有同义近义反义），并自动落在它所属那张图；从家族里点任一词回词中心时，仍停在原来那张图上。
- **详情卡随图裁剪**：同义图只列同义、反义图只列反义、近义图列近义 + 辨析、派生图列派生词 + 本词词缀。卡底固定「查词条 →」，与查词页 `#/word/<词>` 互通。

## 数据怎么来的（不编造关系）

- **词义关系**：274 组高考核心高频词族，手工整理（同义 153 / 近义 55 带辨析 / 反义 66）。近义组都带高考易混辨析：affect/effect、rise/raise/arise、cost/spend/take/pay、borrow/lend、lie/lay…
- **词缀派生 1256 对**：按词形规则计算，与高中词书逐词比对，**只有「去掉词缀后余下部分仍是词表中的词」才成立**（含去 e / 去双写 / i↔y 三种安全拼写还原）。关系只声明词形派生，不声明词义——`really` 按 `re-` 配到 `ally` 这类词形巧合有意为之，页面上也写明了这个口径。
- **逐词释义**：惰性加载高中词书（含音标与双语例句）；词书未收的词由通用词典兜底。

## 页面里还有什么

- 探索框（输入/回车即时展开）、「随机一个词」。
- **词缀表**速查（28 前缀 + 26 后缀，点任意词缀展开家族）——五图共用，保留在页面底部。
- **全部词族**列表：按同义/近义/反义筛选 + 关键词搜索，每个词都可点。
- 夜空视觉：星形星点 + 闪烁 + 中心辐射线 + 相邻星虚线链 + 固定种子背景星野。坐标零随机，同一中心同一图渲染逐字节一致（快照护栏依赖这点）。`prefers-reduced-motion` 下自动关掉闪烁。

## 改动文件

| 文件 | 作用 |
|---|---|
| `data/vocab/synonyms.js` | 词族数据 `window.__SYN__`（由 `.work/build_synonyms.py` 生成） |
| `js/ui/synonyms.js` | 星图集主体：五图状态、页签、翼/圈布局、门户星、随图裁剪的详情卡 |
| `css/synonyms.css` | 页签 + 五图配色（新增后缀青）+ 门户星 + 详情卡新块 |
| `js/app.js` | `/synonyms` 路由 + `syn-explore`/`syn-random`/`syn-star`/**`syn-map`**/`syn-filter`/`syn-search` 动作 |
| `js/ui/library.js` | 首页入口 |
| `index.html` | 引入 `synonyms.js` / `synonyms.css` / `ui/synonyms.js` |
| `.work/domdump.html` | 护栏 ROUTES 含 `#/synonyms` |
| `.work/syn_probe.html` | 五图探针（已按新交互重写，56/56） |

## 验证（护栏实测，2026-08-31）

- 改动的 JS 全部 `node --check` 通过；旧的 `syn-star-aff`/`syn-line-aff`/`syn-dot-aff` 星色引用已清理干净。
- **`.work/syn_probe.html` 56/56 PASS**（走真实点击，不只看 DOM）：页签数量、切图保中心、每图单色单关系、近义图出辨析、前缀图全星 x<320（左翼）/ 后缀图 x>320（右翼）、门户星可点进家族、词缀中心三词义页签灰置且点了不生效、家族词点回词中心保持所在图、空图引导 + 一键跳转、详情卡随图裁剪、「查词条」链到 `#/word/<词>`；派生索引 1256 对与夜空视觉回归正常。
- **45 路由快照：仅 `#/synonyms` 变化，其余 44 条逐字节零回归**；新页面渲染真实内容（146KB，含五页签 + 中心星 + 详情卡 + 查词条链接，非错误页）。
- 交叉回归：`word` 探针 42/42、`kg` 探针 16/16 全过——确认改 `app.js` 没带坏既有交互。
- 基线已重设为 45 路由（旧基线留档 `.work/snap-rev-pre-synmaps.json`）。

## 途中抓到并修复的一个全站级 bug（2026-08-29，已写入项目记忆）

给 `app.js` 的 `click(e)` 加交互时写了 `el.matches(...)`，但 `el` 只在 `change`/`input` 里声明、`click` 里根本没有 → **每次点击都抛 `ReferenceError`，全站所有按钮（含已做好的自评/筛选）整批失效**。渲染不受影响，护栏快照零回归也发现不了，靠同窗口实测点击才暴露。修复：在 `click(e)` 开头补 `const el = e.target;`。
**教训**：护栏只比对 DOM 输出，交互必须单独实测——本项目此后每次改 `app.js` 都跑探针交叉验证（本轮 56/56 + 42/42 + 16/16 即为此）。

## 如何体验

打开 `index.html` → 首页「同义近义反义」入口，或地址栏 `#/synonyms`。

- 输入 `happy` → 同义图 5 颗星；点「后缀」页签 → 右翼展开 `happiness`/`happily`，中心左边挂着 `-ness`（点它看整个家族）。
- 输入 `big` → 同义图 5 颗；点「后缀」页签是空的，会直接告诉你「去同义图看 5 颗星」。
- 点任意星换中心，图不变、中心变——关系链可以一直串下去。

## 可选后续

- 同义替换写作训练：给一句中文/英文，让学生从词族里选最贴切的词（提分完形/写作）。近义图的一对一辨析正好可以直接当题库。
- 词族数据继续扩充（目前 274 组，覆盖绝大部分高频考点）。
- 图上星数超 14 时会截断（页签仍显示真实总数），若常遇到可考虑分页或按词频排序。
