# 高考英语真题在线 · 最终用户体验审查报告

审查日期：2026-09-03
审查视角：不熟悉代码实现的普通用户（高中生 / 家长 / 教师）
审查方式：通读 7,927 行 JS + 11 个 CSS 后，用无头 Chrome 对 22 个真实场景做了三轮实测（`.work/uxtest*.html`）

**结论先行**：项目的功能完整度、空状态覆盖、错误兜底、数据合并口径都做得相当扎实，属于少见的严谨实现。
但存在 **2 个会造成「用户以为成功、实际失败」的 P0 问题**，全部经过实测确认，建议优先修。

> **✅ 修复记录（2026-09-03 当日完成）**：本报告 15 项已全部修复并验证。
> 修复后用无头 Chrome 重跑 15 项断言（`.work/uxfix.html`），**15/15 PASS**；
> 53 路由 DOM 快照护栏重新比对（差异逐条核对本报告改动，无回归）后已重置基线
> （旧基线留档 `.work/snap-rev-pre-uxfix.json`）。各问题的修复方式见文末「附：修复对照表」。

---

## 附：修复对照表（2026-09-03）

| # | 问题 | 级别 | 修复方式（关键落点） |
|---|---|---|---|
| 1 | 存储失败静默 | P0 | `_save()` 置 `Store.writeFailed`；每轮渲染后 `App.afterRender()` 补挂红色警示条（可跳转导出）；成绩页加红字说明 |
| 2 | 空格键全局劫持 | P0 | `hotkey()` 补排除 `a,button,summary,[role=button],[tabindex]`（`tabindex=-1` 除外） |
| 3 | 刷新重置计时 | P1 | 开考时刻落 `Store.saveExamClock`，`remaining` 由挂钟推导；交卷时清钟 |
| 4 | 归零静默交卷 | P1 | 5 分钟 / 1 分钟两档 toast 预警（aria-live）；成绩页显示「已自动交卷」说明（`record.autoSubmitted`） |
| 5 | urgent 永不生效 | P1 | tick 里 `classList.toggle('urgent', remaining<300)`；计时器加 `data-exam-timer` 限定只更新练习页 |
| 6 | 清空错题不可逆 | P2 | 内存备份 + 10 秒撤销条（`UI.undoBar` / `Store.restoreMistakes`） |
| 7 | addWord 拒词组无反馈 | P2 | 正则放宽（上限 40 字符）；返回布尔，失败时 toast 提示 |
| 8 | 查词框无按钮 | P2 | 词首页与词条页补「查询」按钮，空输入请焦点回输入框 |
| 9 | 考试中离开无挽留 | P2 | `beforeunload` 挽留（仅 `Exam.timer` 存活时） |
| 10 | 首页无草稿提示/重做入口 | P3 | 草稿横幅（复用续做条样式）+ 行状态三态：继续作答 · 已答 N 题 / 上次 X 分 · 重做 / 开始练习 |
| 11 | 导入无预览即合并 | P3 | 拆两步：选文件先预览「即将导入什么」，确认后才 `mergeData`，可取消 |
| 12 | 错误页抛英文堆栈 | P3 | 已知错误给中文说明；未知错误中文兜底 + 技术细节默认折叠 |
| 13 | 无记录成绩页静默跳转 | P3 | `UI.noRecord` 说明页（有草稿时报「已答 N 题」，给继续入口） |
| 14 | 主观题显示「正确答案：」 | P3 | 写作/改错改为独立「参考范文」块 |
| 15 | 试卷行省份过长 | P3 | 超 3 个省份折叠为「前 3 + 等 N 个省份」（`UI.regionText`） |

新组件样式集中在 `css/components.css` 末尾「UX 修订组件」段；全部走令牌、遵守 P-38（整块底色+边框，无左竖线）。

---

## 一、问题总览

| 级别 | 数量 | 定义 |
|---|---|---|
| **P0** | 2 | 数据丢失或某类用户完全无法操作 |
| **P1** | 3 | 限时考试这个核心场景的体验失效 |
| **P2** | 4 | 常见误用、不可逆操作、操作无反馈 |
| **P3** | 6 | 信息缺失、文案误导、边界未处理 |

实测方式说明：用无头 Chrome 加载真实页面，在 iframe 内通过 `eval` 操作词法作用域内的 `Exam` / `Store`（它们是顶层 `const`，不挂在 window 上），模拟真实用户操作后读回 DOM 与状态。

---

## 二、P0 · 数据与操作阻断（建议立即修）

### 1. 存储写入失败完全静默，学生会看到一份刷新后就不存在的成绩

**现象**
`js/store.js` 的 `_save()` 在写入失败时（隐私模式、配额满、`file://` 下部分浏览器）只做 `console.warn` 并 `return false`。
而**所有调用方都不检查返回值**——`Exam.set` / `saveRecord` / `addMistakes` / `markMistakeReviewed` / `addWord` 全都不检查。

**实测**
```
模拟 localStorage 配额耗尽 → Store._save() 返回 false
强制写入失败后交卷 → 页面无任何提示，照常显示成绩
```

**用户会经历什么**
学生花 120 分钟做完一套卷，点交卷，看到成绩页，「客观题得分率 78% · 用时 96 分钟」。
页面顶部还写着「答题过程中会自动保存草稿」。
他关掉浏览器，第二天打开——**什么都没有了**。成绩、错题、间隔复习排期全部消失，且从头到尾没有任何提示。
同样的情况也会发生在错题本标记、生词本收藏上。

**为什么严重**
这不是「功能没做」，而是**系统向用户谎报了成功状态**。用户没有任何机会补救（比如导出备份）。

**改进建议**
1. `_save()` 失败时置一个全局标志 `Store.writeFailed = true`；
2. 在各页面顶部渲染一条持久的红色警示条：
   > 当前浏览器无法保存进度（可能是隐私模式或存储空间已满）。请立即到「数据备份」导出，否则关闭页面后记录会丢失。
3. 首次失败时额外弹一次明确说明，不要只靠警示条；
4. `Exam.submit()` 里检查写入结果，失败时不要照常跳成绩页。

---

### 2. 空格键被全局劫持，键盘用户在闪卡页完全无法操作

**现象**
`js/app.js` 的 `hotkey()` 里，空格键处理是：
```js
if (k === ' ') {
  const btn = document.querySelector('[data-action="phr-flip"]');
  if (btn) { btn.click(); e.preventDefault(); }
  return;
}
```
判空条件只排除了 `input,textarea,select` 和 `isContentEditable`，**没有排除 button 和 a**。
只要页面上存在翻卡按钮（闪卡页有到期卡片时必然存在），任何非输入元素聚焦时按空格都会被 `preventDefault` 并触发翻卡。

**实测**
```
页面存在翻卡按钮时，聚焦「移除」按钮按空格：
  defaultPrevented = true，翻面触发 1 次，移除触发 0 次
聚焦链接按空格：
  defaultPrevented = true，链接未被激活
```

**用户会经历什么**
用键盘的学生在词组闪卡页 Tab 到「移除」按钮，按空格——卡片翻面了，移除没发生。
再按一次，还是翻面。他永远无法用键盘完成任何操作，只能改用鼠标。
读屏软件用户、运动障碍用户受到的影响更彻底。

**改进建议**
把判断收紧为「焦点不在任何可交互元素上」才生效：
```js
if (k === ' ') {
  if (document.activeElement && document.activeElement !== document.body) return;
  const btn = document.querySelector('[data-action="phr-flip"]');
  if (btn) { btn.click(); e.preventDefault(); }
  return;
}
```
同样的收紧也应应用到 A–D 快捷键分支（当前同样未排除 button/a 聚焦的情况）。

---

## 三、P1 · 限时考试核心体验失效

### 3. 刷新页面即可重置计时，限时形同虚设

**现象**
`js/exam.js` 的 `start()` 每次进入答题页都执行 `this.remaining = (exam.duration || 120) * 60`，已用时间不做持久化。

**实测**
```
已消耗到 remaining = 2999s
重新进入答题页 → remaining = 7200s（满值）
```

**影响**
- 「限时模考」这个核心卖点失效：做到一半刷新，时间重来；关掉几小时再回来，又是 120 分钟。
- 连带**用时统计失真**：`durationUsed = 满时长 - remaining` 是按重置后的计时倒推的，成绩页的「用时 X 分钟」不可信。
- 诚实的学生吃亏，熟悉浏览器的学生获得无限时间，成绩之间不可比。

**改进建议**
把 `startedAt`（或剩余秒数）与草稿一起存进 localStorage，`start()` 时若发现未交卷记录就续算。
如果产品上希望保留「刷新不惩罚」的宽松体验，那就在答题页明确写出当前口径，并停止在成绩页展示「用时」。

---

### 4. 时间归零直接跳走，无预警也无事后说明

**实测**
```
把剩余时间设为 2 秒 → hash 直接从 #/exam/... 变为 #/result/...
全程无「还剩 5 分钟」提醒，跳过去后也无「时间到，已自动交卷」说明
```

**影响**
正在写作文的学生会瞬间被切到成绩页。内容虽然已存草稿，但突然被打断的体验很差，且不知道发生了什么。

**改进建议**
- 剩 5 分钟、剩 1 分钟各给一次非阻塞提醒（用 `aria-live` 区域，读屏也能收到）；
- 自动交卷跳转到成绩页时，在页面顶部加一条说明：「考试时间已用完，系统已自动交卷」。

---

### 5. 倒计时剩 5 分钟不会变红——样式写了但从未生效

**现象**
`.timer.urgent { color: var(--red) }` 样式存在，`examBar()` 里也写了 `remaining < 300 ? 'urgent' : ''`。
但每秒的 tick（`Exam.start` 的 setInterval）只 `replaceChildren` 替换文本节点，**不更新 class**。

**实测**
```
把 remaining 设为 200s，等一次 tick：
  文本正确变成 "03:18"，但 class 仍是 "timer "，无 urgent
```

**影响**
除非学生打开页面时就已经剩不到 5 分钟，否则永远看不到时间告警。这个警示功能等于没有。

**改进建议**
tick 里同步切换 class：
```js
const t = document.querySelector('.timer');
if (t) { t.replaceChildren(document.createTextNode(UI.time(this.remaining)));
         t.classList.toggle('urgent', this.remaining < 300); }
```
改动极小，与第 4 条属于同一个修复面。

---

## 四、P2 · 常见误用与不可逆操作

### 6. 清空全部错题只有一次原生弹窗，无撤销、无备份提醒
**实测**：造 3 条错题后点清空，触发 confirm 1 次。
**影响**：错题本里的间隔复习排期（reviewStage / nextReviewAt）是用时间累积换来的资产，一次误点全部永久删除。
**建议**：改用站内确认弹层，要求输入「清空」二字或勾选确认，弹层内放「先导出备份」链接；删除后给 10 秒撤销条。

### 7. 收藏生词遇到词组 / 含数字词形静默失败
**实测**：`Store.addWord('look after')` → 0 条变化；`addWord('COVID-19')` → 0 条；`addWord('abandon')` → 1 条。
原因：`addWord` 的正则 `/^[a-z][a-z'-]*$/` 不接受空格与数字，失败时直接 `return`，无返回值、无提示。
**影响**：用户点「☆ 收藏生词」，页面纹丝不动——最典型的「点了没反应」。
**建议**：放宽校验允许空格与数字；失败时给按钮就地反馈（如「这个词形暂不支持收藏」），至少要让用户看到点击被接收。

### 8. 查词框没有可见的查询按钮
**实测**：输入框同一行内没有任何按钮或可点元素，只能靠回车或失焦触发。
**影响**：查词是高频入口，新用户不知道要按回车；移动端若软键盘不出现「前往」键就直接卡住。
**建议**：加一个「查询」按钮，回车与点击都能触发（同义词星空页的探索框已有按钮，可复用同一套结构）。

### 9. 考试进行中误点返回 / 关闭标签页无任何挽留
**实测**：答题页顶栏品牌链接直接指向 `#/`，点一下即离开；全站无 `beforeunload` 处理器。
**影响**：草稿虽保住了，但计时归零（见第 3 条），且用户没有任何「你正在考试中」的提示。
**建议**：考试进行中注册 `beforeunload`；顶栏品牌链接改为「返回题库」并加确认。

---

## 五、P3 · 信息缺失与文案

### 10. 首页看不出哪套卷有未完成草稿，也没有「重做」入口
**实测**：造了一条整卷草稿后，首页真题区无任何「继续 / 未完成 / 草稿」提示；首页也没有「重做 / 清空」字样。
专题页有续做横幅，整卷没有。学生忘了做到哪套，更麻烦的是想重做一套做过的卷子（清空旧答案）完全没有入口。
**建议**：试卷行在有草稿时显示「继续练习（已答 N 题）」，有成绩时给一个「重做」入口。

### 11. 导入进度无确认步骤，选错文件立即合并
选中文件即刻合并，无预览、无确认、无撤销。合并口径本身是安全的（取进展更靠前的一方），但用户无法预知会改什么。
**建议**：解析成功后先展示「将新增 X 条错题、更新 Y 套成绩」的预览，确认后再写入。

### 12. 错误页把技术信息直接抛给用户
`renderError` 直接渲染 `err.message`，用户看到的是 `Cannot read properties of undefined (reading 'sections')` 这类英文堆栈；访问失效链接显示「题库加载失败」，用户会以为网站坏了而不是链接失效。
**建议**：错误分两类——已知业务错误给中文说明 + 下一步操作；未知错误才附带技术细节（默认折叠）。

### 13. 访问没有成绩记录的卷子被静默重定向
`#/result/<id>` 无记录时直接 `location.replace` 到答题页，无任何解释，且历史记录被替换，后退也回不去。
**建议**：显示「这套卷还没有成绩记录，先做一遍吧」的说明页，并给一个明确的入口按钮。

### 14. 写作题与短文改错在解析页显示成「正确答案：」
**实测**：`gk2021-jia` 的 q71（短文改错）与 q72（应用文写作）都带 `modelAnswer`，解析页会渲染成「正确答案：Dear Chris, …」。
**影响**：主观题没有唯一正确答案，这个措辞会让学生误以为要背下这篇范文；而自己的作文既没被判分，也没有自评引导。
**建议**：主观题改用「参考范文」/「参考批改」，并在旁边补一句话说明这部分不计入机器评分。

### 15. 首页试卷行信息过长
`region` 字段把 11 个省份全列出来（「广东、福建、湖南、湖北、江苏、河北、山东、浙江、安徽、江西、河南」），紧接着又跟「短文改错已人工确认」徽标，窄屏上会挤成一团。
**建议**：省份超过 3 个时折叠为「广东等 11 省市」，完整信息放 title 属性或详情页。

---

## 六、做得好的地方（值得保留，不要改坏）

审查中也确认了一批扎实的实现，重构时请一并保住：

- **空状态覆盖完整**：22 处空态文案都给了下一步指引，而不是干巴巴的「暂无数据」（如生词本空态会说明「在查词或错题解析里遇到生词，点收藏生词就进这里」）。
- **加载态齐全**：查词、星图、专题、考点透视、节点练测都有「正在…」的明确文案，异步回填时还会检查页面是否已切走。
- **跨设备合并口径严谨**：`mergeData` 的 11 个集合各自有明确的合并策略，间隔复习排期「取进展更靠前的一方」这个设计考虑得很周到。
- **导出页如实告知边界**：明确写出「写作草稿不在此列」，没有让用户产生错误预期。
- **数据一致性好**：8 本词书 26,000+ 条目全部有释义字段，不存在渲染出 `undefined` 的边界。
- **答题页不开放查词**：`data-lookup` 只在解析模式挂载，避免边考边查。
- **响应式无障碍基础良好**：浮动答题卡按钮按断点切换，`aria-live` / `aria-expanded` / `role="status"` 都有使用。

---

## 七、建议修复顺序

| 批次 | 问题 | 预估改动 |
|---|---|---|
| 第一批（同一处代码） | 3 / 4 / 5 —— 计时续算 + 到点提醒 + urgent 样式同步 | `js/exam.js` 约 20 行 |
| 第二批 | 1 —— 存储失败可见化 | `js/store.js` + 一处共用警示条组件 |
| 第三批 | 2 —— 空格热键收紧 | `js/app.js` 约 4 行 |
| 第四批 | 6 / 7 / 8 / 9 —— 破坏性操作防护与操作反馈 | 4 处局部改动 |
| 第五批 | 10–15 —— 文案与信息补全 | 6 处局部改动 |

前三项建议一起做：它们都集中在考试主流程上，是这个产品最核心的使用路径。

---

附：实测脚本保留在 `.work/uxtest.html` / `uxtest2.html` / `uxtest3.html`，修改后可直接重跑验证。
