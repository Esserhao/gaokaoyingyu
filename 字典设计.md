# 字典模块设计

> 目的：给全站提供可点击查词的英汉字典，并把「词汇」这一块从知识体系里独立出来。
> 相关文档：`task.md`（总路线）、`题库待补清单.md`（数据缺口口径）、`目录说明.txt`。
> **本文件是设计稿，尚未开始实现。** 已定与待定分节标注，待定项答定后回填第三节。

---

## 一、为什么把词汇单独拆出来做字典

按 `js/diagnose.js` 的规则表跑全库 955 道客观题（16 套卷，跳过写作与短文改错），
考点实际分布是：

| 家族 | 节点数 | 题量 | 占比 |
|---|---|---|---|
| 篇章能力（细节理解 130 / 听力细节 126 / 推理判断 120 / 语篇衔接 62 / 主旨大意 42 / 场景与人物 20 / 词义猜测 14 / 观点态度 5） | 8 | 519 | **54.3%** |
| 词义辨析（动词 108 / 名词 83 / 形容词 40 / 副词 13） | 4 | 244 | **25.6%** |
| 语法与词形（非谓语 48 / 连词 21 / 固定短语 15 / 介词 14 / 冠词 12 / 定语从句 12 …共 21 项） | 21 | 192 | **20.1%** |

结论有两条：

1. **语法只占五分之一。** 知识体系那套五级台阶（变体闸门、strength 分级、
   单元级 mini 诊断）只服务这 192 道题，硬套到阅读听力上会造出
   「等级永远为 0」的空节点。篇章能力因此另立门户，进 `#/training` 题型专项。
2. **词义辨析占四分之一，但它不是「规则」。** `动词词义辨析` 写不出可背的条文，
   8–10 道题也覆盖不了它的变体空间。所以拆成两半：
   **方法归题型专项**（完形填空下的四个子技能），**词条归字典**（本文件）。

## 二、语料实测（2026-08-29 测得，复现口径见第七节）

统计范围：`data/exams/*.json` 的 `passages[].text` + `questions[].stem`
+ `options[].text` + `questions[].transcript`。

| 指标 | 数值 |
|---|---|
| 总词次 | 45 974 |
| 不同词形（小写归一） | **6 459** |
| 其中专有名词类（句中仍大写、且句中从不以小写出现） | 750 |
| 普通词形 | 5 709 |
| 只出现 1 次的普通词形 | 2 269 |
| 词 — 出现位置 对 | 23 422 |
| 位置索引（词 → 试卷/题型）JSON 体积 | 约 560 KB |
| 首字母分片最大片（s） | 723 词 |

再往下拆（同一份语料）：

- **词形 → 词条**：用后缀还原（-s/-es/-ed/-ing/-er/-est/-ies/-'s，含双写与哑 e 还原）
  跑一遍，1 799 个词形能折叠到库内另一个词形上，**实际需要独立词条的约 4 706 个**。
  即「收录全部 6 459 个词形」在内容工作量上等于「写 4 706 条释义 + 1 799 条跳转」。
- **形近词**：长度 ≥4、出现 ≥2 次的 3 482 个词里，编辑距离 1（替换或删除一个字母）
  且不属于同一词条屈折形式的配对有 **1 403 组**。机器全出会有大量噪声
  （`abby`/`baby`、`able`/`alex`），必须过滤，见第六节 G 项与 D9。
- **近义/易混词**：275 道完形填空的选项组是命题人亲手分好的易混词簇
  （如 `['surprised','pleased','excited','worried']`、`['dug','cleared','cut','laid']`），
  共 926 个不同选项词，147 个在两道以上题目里重复出现。这是站内**唯一权威**的
  近义词来源，且免费。
- **释义**：847 条带解析的题目里，能机械抽出「英文词 = 中文释义」的配对只有
  **42 对**（如 `roles 指职责、任务`）。**释义必须外部引入或人工写，项目内没有数据源。**

## 三、已定决策（2026-08-29 定）

| # | 决策 | 内容 |
|---|---|---|
| D1 | 词义辨析题的归宿 | 244 道题拆成**完形填空下的四个子技能**（动词看搭配与语气 / 名词看指代与复现 / 形容词看褒贬 / 副词看逻辑关系），进 `#/training` 题型专项；题池直接用 275 组现成选项。字典建成后从子技能页链向词条 |
| D2 | 题型专项的度量 | 子技能三档：**生疏 <50% / 在练 50–79% / 稳定 ≥80%**，按该子技能最近 10 次作答算。故意与知识体系的五级台阶不同名，避免两套进度被学生混成一套 |
| D3 | 地址空间 | 不新开 `#/skill/*`。扩建现有 `#/training`；`#/knowledge/<节点>` 仍是错题本的唯一出口，按「节点 → 家族」映射表分派三种版式（语法节点页 / 跳题型子技能 / 跳字典词条） |
| D4 | 词块的归属 | 知识体系只留 **`固定短语` 一个节点**，教的是「词块错误怎么自查」的方法；`be fond of`、`the way in which` 这类具体词条清单归字典。（`固定短语` 是 `js/diagnose.js` 必然产出的节点名，没有页面就是死链，故不能取消） |
| D5 | 首批收词范围 | **全部 6 459 个词形**（≈4 706 个独立词条 + 屈折形式跳转）。理由：短期不上线，可以打磨到更好再传 |
| D6 | 释义的正确性闸门 | 与知识节点**共用同一套 `verified` 口径**：每条释义必须带 `source`，未经人核显示灰色「待校」。理由：上一版知识库正是靠「这么基础的东西不会写错」垮掉的（19 条 `rule` 全空、that/which 规则写反），而一条错释义会被学生当权威背下去 |
| D7 | 释义来源 | **外部词典库打底 + 人工只校订高频层**。全量导入设 `verified: false`；人工核订范围＝出现 ≥3 次的实词（约 2 385 个）+ 全部 926 个完形选项词 ≈ 3 300 条；其余长尾（2 269 个只出现 1 次的词形，多为专有名词与低频派生词）维持灰标「待校」。**如实标灰，不假装可信** |
| D8 | 许可证核实 | **阻塞项**。在人工确认候选库的 `LICENSE` 与 `tag` 字段取值之前，不写导入脚本、不按任何特定库的字段结构设计数据格式（否则换库＝导入与分片全部重做）。本轮尝试核实 ECDICT 失败，见第八节 |
| D9 | 形近词生成 | 机器规则先收窄（双方均为出现 ≥3 次的实词、且首字母相同），再由人工补经典对（`quite`/`quiet`、`affect`/`effect`）。**人工用在「补」而不在「筛」**——机器原始配对 1 403 组噪声过大（`abby`/`baby`、`able`/`alex`） |
| D10 | 「点击翻译」范围 | 分阶段。第一期：解析页 + 错题本 + 字典页可点。题目正文可点列为后续 TODO——它要改 `UI.text()` 的输出，而输出受 37 路由 DOM 快照逐字节护栏约束；且**做题时正文可点等于给学生一个合法的偷看入口** |
| D11 | 生词本 | 做。`gkyy_records_v1` 新增 `words` 区（收藏词 + 加入时间 + 来源题目），接 `js/review.js` 现成的 1/3/7/15/30 阶梯做背词排期。新写一条 `syncMergeWords`（口径同 `syncReviewMoreAdvanced`：取更靠前的进度）。补的是真实缺口——现在因不认识 `abandon` 而错的题，回炉复习的是整道阅读题，不是那个词 |
| D12 | 词条地址 | `#/word/<小写词形>`，屈折形式自动跳主词条。理由同知识节点保留中文 id：**地址里放学生认得的东西** |
| D13 | 运行时不建数据库 | 见第四节。`file://` 下 `fetch`/`XHR` 被拦，SQLite（sql.js/WASM）必须把 `.db` 当二进制读进来，这条路走不通。数据库只用于**构建期**，运行时一律静态 `.js` 分片 |

<!-- CHUNK4 -->

## 四、为什么不建运行时数据库（D13）

别的项目用 `.db`/`.csv`，是因为它们有服务端或本地进程去查。本项目两条硬约束把这条路封死：

1. **`file://` 下取不到二进制。** 在浏览器里跑 SQLite 只有 sql.js / wa-sqlite（WASM）一条路，
   而它们都要先把 `.db` 整个读成 `ArrayBuffer`——手段是 `fetch` 或 `XHR`，
   两者在 `file://` 下都被 CORS（`origin 'null'`）拦死。这正是本项目当初把
   `data/*.json` 全部改成 `<script>` 注入 `.js` 的原因（见 `js/data-loader.js` 头注）。
   `<script src>` 不受此限，但它只能加载 JS，加载不了 `.db`。
2. **零构建。** 引入 WASM 运行时（sql.js 约 1 MB）＋异步初始化，
   为的是查一张 4 706 行的表——而 4 706 个键的 JS 对象查询是 O(1) 哈希，
   本来就比 SQL 快。

**实测规模（占位释义，按第二节口径生成 4 706 条词条）：**

| 文件组 | 体积 |
|---|---|
| 词条分片 `shard-<a..z>.js` 合计 | **0.79 MB**（最大片 `s`：524 条 / 90 KB） |
| 位置索引 `occur-<a..z>.js` 合计 | **0.58 MB**（最大片 `s`：59 KB） |
| 薄索引（词形 → 分片字母），需预加载 | **103 KB** |

对照现状：`data/exams/*.js` 已有 **0.98 MB**，单套卷 53–70 KB，
预加载的只有 `data/index.js`（5.5 KB）、`knowledge.js`（24.5 KB）、`knowledge_graph.js`（5.2 KB）。
**字典最大分片 90 KB 与一套卷同量级，按需注入完全在既有承受范围内。**

薄索引 103 KB 是唯一需要预加载的新增量。若嫌大，可退化成「只存排序后的词形列表」
（71 KB），分片字母由 `word[0]` 现算——代价是屈折形式跳主词条要多一次猜测。
**待定：见第六节 G7。**

**数据库仍然有用，但只在构建期。** 导入外部词典、跑一致性检查、生成分片
用 SQLite/pandas 都合适，产物是 `.js` 静态文件。这与现有流水线一致：
`tools/` 下已是「Python 处理 → 生成静态数据」的形态，
`.work/gen_data_js.py` 做的就是 `JSON → JS`。字典只是多一道导入环节。

<!-- CHUNK5 -->

## 五、数据形态（受现有铁律约束）

**铁律：本站零构建、需支持 `file://` 双击打开，因此本地数据一律不能用 `fetch` 读**
（CORS `origin 'null'` 会直接抛 `Failed to fetch`，整站卡在「页面加载失败」）。
数据必须走 `.js` 文件赋值全局 + `<script>` 按需注入，见 `js/data-loader.js`。

位置索引整包约 560 KB，不能预加载，按首字母分片（最大片 `s` 723 词）：

```text
data/dict/index.js            → window.__DICT_INDEX__   词形 → 分片号 + 词条 id（薄索引，预加载）
data/dict/shard-<a..z>.js     → window.__DICT_CACHE__['<a..z>'] = { 词条… }（按需注入）
data/dict/occur-<a..z>.js     → window.__DICT_OCCUR__['<a..z>']  = { 词 → [试卷/题型…] }（按需注入）
```

注入器仿 `js/data-loader.js` 的 `loadExamData`：keyed 缓存 + `onload`/`onerror`，
失败按空词条降级（`js/ui/base.js` 的 `loadKB` 是既有先例：加载失败不许白屏）。

词条字段草案：

```json
{
  "word": "abandon",
  "forms": ["abandoned", "abandoning", "abandons"],
  "phonetic": "əˈbændən",
  "senses": [
    { "pos": "v.", "zh": "放弃；抛弃", "source": "<词典名/出处>", "verified": false }
  ],
  "confusable": ["desert", "quit"],
  "lookalike": ["abundant"],
  "occurrences": "见 occur-a.js"
}
```

`senses[].verified` + `senses[].source` 与知识节点同一口径（D6）。
`confusable` 首批只从完形选项组生成（有命题人背书）；`lookalike` 见第六节 G 项待定。

## 六、待定项（下一轮 grill 的题目）

- **G5 音标与发音**：音标是否必填、是否引入发音音频（`audio/` 现用于听力音频）。
  音标若来自外部库则受 D8 阻塞；发音音频有体积与来源两重问题。
- **G7 薄索引的形态**：完整映射（词形 → 分片字母，103 KB，预加载）
  还是只存排序词形列表（71 KB，分片字母现算）。差 32 KB，换的是屈折跳转的确定性。
- **G8 长尾灰标的呈现**：2 269 条「待校」词条在词条页上怎么显示才既诚实又不劝退。
- **G9 4 706 条的核订批次**：按字母、按频次、还是按题型来源切批？
  建议放到知识体系与题型专项上线后再定——那时才知道逐条核验的真实速度。

## 七、复现统计的脚本

第二至四节所有数值可用一段独立脚本复算，遵循 `tools/` 下现有脚本的惯例
（`python tools/<name>.py`，UTF-8 输出用 `io.TextIOWrapper` 包 `sys.stdout`，
否则 Windows 终端下中文输出会乱码）。待实现：`tools/dict_survey.py`。

统计口径固定为：`data/exams/*.json` 的 `passages[].text` + `questions[].stem`
+ `options[].text` + `questions[].transcript`，词形正则 `[A-Za-z][A-Za-z'-]+`，
小写归一；专有名词判定＝「句中出现过大写、且句中从未以小写出现」；
词条折叠＝后缀还原 `-s/-es/-ed/-ing/-er/-est/-ies/-'s`（含双写与哑 e）。

## 八、候选词典库核实清单（D8 阻塞项，需人工完成）

对每个候选源确认三样，缺一不可：

1. **LICENSE 文件的实际内容**（不看第三方摘要、不看镜像站）。需允许再分发与
   在本项目内使用；若要求署名，确定署名位置。
2. **`tag` 字段的取值列表**——是否存在「高考」一档。若只有 CET4/IELTS 等，
   则收词范围要另找依据（可退回本站语料自身的频次分层）。
3. **对本站 4 706 个词条的覆盖抽样**——随机取 100 个（含专有名词与派生词）查命中率。

已尝试未果的源：

| 源 | 状态 |
|---|---|
| ECDICT（skywind3000） | 声称数十万词条、带考纲标签与 `exchange` 词形变化字段，看起来正合需要。但 **LICENSE 与 `tag` 是否含「高考」两点均未核实** ——GitHub / SourceForge / DocSearch 三域在开发环境被网络策略拦截，读不到原始 README。搜索摘要中只见 CET4、IELTS。**不得据此假定可用。** |


